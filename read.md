# Project State

## Workflow Preferences
- **Auto-commit and auto-push**: the user has given standing authorization to commit *and push* every change to git automatically, without asking first each time. Keep committing in logical, reviewable chunks (per feature/game/fix, not one giant commit) the same way this session's work was organized, then push after committing. This is scoped to the working branch (`feature/full-game-platform` as of this writing) — opening PRs or merging to `main` still needs explicit confirmation. Don't commit code that doesn't type-check or build.

## Current State
The platform now has **all 10 Bible-centered games from the spec fully built and live**: Bible Quiz Levels, Memory Verse, Word Search, Scripture Puzzle, Flash Cards, Match the Verse, Bible Trivia, Bible Story Challenge, Character Guessing Game ("Who Am I?"), and Daily Bible Challenge. Every game that has a natural "levels" concept (all except Bible Quiz, which the user built separately, and Daily Challenge, which is inherently once-a-day) now ramps difficulty across roughly 20 rounds, using real authored content — no fabricated or placeholder game content. Every Bible verse anywhere in the app is the exact King James Version text, verified word-for-word, not paraphrased. Every game that has a "finish a level" moment now shows a shared trophy completion screen (stars, level score, XP earned, and running **cumulative** total score pulled from the player's real `xp`, not just the round's score) with a "Continue" action into the next level/round. The home page is a full marketing-style landing page (hero, mission, features, how-it-works, 10-game showcase, level-system explainer, audience highlights, FAQ, footer) with a `royal`/`gold` color identity layered on top of the original palette. Both backend and frontend type-check cleanly and pass full production builds.

## Completed Work
- Reviewed `prep.md`, `claude.md`, and `instruction.md`.
- Mapped the existing backend scaffold.
- Saved the implementation plan in `chat.md`.
- Built the backend foundation for steps 1 to 5.
- Added Prisma models for categories, difficulty levels, media assets, quiz questions, verses, admin users, sessions, and import jobs.
- Added shared middleware, validation, error handling, and Prisma client setup.
- Added admin auth, CRUD content APIs, and JSON import/seed scripts.
- Verified the backend with `prisma generate` and `tsc`.
- Added player profiles, progress logs, game sessions, and game answers.
- Added quiz session creation and answer submission APIs.
- Added memory verse session creation and answer submission APIs.
- Added badges and avatar unlock models with automatic reward evaluation.
- Added paginated admin list endpoints and import history.
- Re-validated the backend with `prisma generate` and `tsc` after the gameplay additions.
- Added a generated Prisma migration file for the expanded gameplay and progression schema.
- Scaffolded a child-friendly Next.js frontend with quiz and memory verse pages.
- Ran a code-level error check on the new frontend files.
- Rebuilt `server/src/swagger.ts`, which had become corrupted (mangled indentation, a duplicated `PlayerProfile` schema key, and code fragments spliced out of order across two files). Reconstructed one clean, valid OpenAPI spec covering all 11 schemas (player, quiz, and memory verse request/response types) from the recoverable fragments in both broken files.
- Deleted `server/src/swagger.min.ts`, a duplicate of `swagger.ts` that was also corrupted and had become the file `app.ts` was actually importing from. `app.ts` now imports from `./swagger` again.
- Installed missing `@types/swagger-jsdoc` and `@types/swagger-ui-express` dev dependencies, which were causing implicit-`any` type errors.
- Removed the stray root-level `package-lock.json` (an empty lockfile with no matching root `package.json`; the project only has `server/` and `frontend/` as real npm packages).
- Removed a bogus `"types": "module"` field from `server/package.json` (invalid duplicate of the correct `"type": "module"` field already present).
- Verified `npx tsc --noEmit` passes with zero errors in both `server/` and `frontend/`, and confirmed the rebuilt swagger spec loads at runtime (11 schemas, 7 documented paths).
- Swept the repo for duplicate filenames outside `node_modules`/`.next`/`dist`; the only repeats found (`package.json`, `package-lock.json`, `tsconfig.json` per package, `page.tsx` per Next.js route, one `migration.sql` per Prisma migration) are all legitimate, not accidental duplicates.
- Confirmed the `play_postgres` Docker container (Postgres 17, port 55432, matches `server/.env`'s `DATABASE_URL`) is up and reachable. `npx prisma migrate status` showed both existing migrations already applied and the schema up to date.
- Ran `npm run seed`: loaded the starter content from `src/data/seed-content.json` (2 categories, 2 difficulty levels, 1 media asset, 2 quiz questions, 1 Bible verse) and logged one import job. Verified row counts directly against the database afterward.
- No admin user was created by the seed, since `ADMIN_BOOTSTRAP_EMAIL`/`ADMIN_BOOTSTRAP_PASSWORD` are intentionally left unset in `server/.env` (the user chose to hold off on setting admin credentials for now).
- Fixed the Quiz answer flow: `QuizPreview`'s option buttons had no `onClick` at all, so answers were never submitted and no feedback ever showed. Wired them to the existing `POST /games/quiz/answers` endpoint, added correct/incorrect visual feedback (✅/❌, color states, an XP/encouragement banner), and passed the real `sessionId` down from `quiz/page.tsx` (it was never being passed before).
- Fixed the identical gap in Memory Verse: the textarea had no submit button and no way to check an answer. Wired it to `POST /games/memory-verse/answers` with the same feedback pattern, and passed the real `sessionId` down from `memory-verse/page.tsx`.
- Introduced a shared `GameAnswerResult` type in `lib/types.ts` since the quiz and memory-verse answer endpoints return an identical shape; avoids duplicating the type.
- Fixed a production-only bug found via `next build`: `/quiz` and `/memory-verse` were being prerendered as **static** pages (`○ Static`). Since loading either page creates a new player and a new game session as a side effect, static generation meant that mutation only ran once at build time — every visitor in a real deployment would have shared the same session and the same locked-in questions, breaking the game after the first person finished it. This didn't surface in `next dev` (which re-runs the page per request) — only in a production build, which is how Vercel deploys it. Fixed by adding `export const dynamic = "force-dynamic"` to both pages. Verified with `next build`: both routes now show `ƒ (Dynamic)`.
- Verified both new answer flows end-to-end against the live backend (not just via `tsc`): created real sessions, submitted a correct and an incorrect answer to each, and confirmed the response shape matches what the UI renders.
- Ran full production builds for both `server/` (`tsc`) and `frontend/` (`next build`) — both clean. Note: running `next build` in the same `.next` folder as a live `next dev` process corrupted the dev server's cache (`MODULE_NOT_FOUND` on `/quiz`, `/`) — killed the dev process, cleared `.next`, and restarted clean. All three frontend routes confirmed back to 200.
- Audited backend auth middleware, error handler, validation middleware, and the admin CRUD service/controllers/routes for correctness — no bugs found; route-to-controller-to-service wiring is consistent and matches the Prisma schema.
- **Built the leveled Bible Quiz system** (8 levels, 25 questions each, 10 pts/question, hints cost -2 pts up to 2 per question, sequential unlock on completion):
  - Schema: added `PlayerLevelProgress` (per player+category: unlocked/completed/bestScore/attempts) and `GameSessionHint` (server-authoritative hint tracking, unique per session+question+hintNumber so scoring can't be spoofed by a client-supplied "hints used" count). Migration generated via `prisma migrate diff` + `migrate deploy` (non-interactive, since `migrate dev` needs a TTY this environment doesn't have) and applied cleanly.
  - Levels map 1:1 onto the existing `Category` model ordered by `sortOrder`: Genesis → Exodus → Kings → Prophets → Parables → Jesus → Miracles → Apostles (roughly increasing difficulty/specificity). Reused rather than adding a new "Level" model.
  - New `server/src/services/levelService.ts`: `listLevelsForPlayer`, `startLevelSession` (locks enforced server-side, not just hidden in the UI), `requestLevelHint` (eliminates one not-yet-eliminated incorrect option, max 2 per question, blocks hinting after the question's already answered), `submitLevelAnswer` (computes points server-side from the hint count on record, awards XP/stars/streak through the same reward pipeline as the rest of the app, marks the level complete and unlocks the next one when all 25 are answered).
  - New routes under `/api/games/levels`: `GET /` (list with lock state), `POST /:categorySlug/sessions`, `POST /hints`, `POST /answers`. New `levelController.ts`, validators added to `gameValidators.ts`, 4 new schemas + these paths added to `swagger.ts` (written carefully this time, given the earlier corruption incident — verified it still loads at runtime).
  - Authored and imported **200 real, accurate trivia questions** (25 per level) into `server/src/data/levels-content.json`, each with a scripture reference and a one-line explanation. Imported through the existing `importContent` pipeline. Verified programmatically: 200 unique slugs, every question has exactly 4 options with exactly 1 marked correct, and each of the 8 categories ends up with exactly 25 *active* questions (deactivated 2 leftover questions from the original minimal starter seed that would otherwise have made Genesis/Jesus show 26).
  - Frontend: new `LevelMap` component (`/levels`) showing all 8 levels with lock/unlock/complete state; new `LevelQuiz` component (`/levels/[slug]`) with a HUD (running score, "Question X of 25", max-points-remaining), lettered A/B/C/D options, a hint button showing hints remaining, and a level-complete screen linking to the next unlocked level. Both are self-contained client components using the same persisted-player pattern as the Quiz/Memory Verse fix below.
  - Home page and Hero updated to make the leveled system the primary "Bible Quiz" entry point; the old `/quiz` (5 random questions, no levels) stays live as a "Quick Practice" link.
  - **Found and fixed a real bug while wiring this up**: `validateQuery` in `middlewares/validate.ts` (previously unused — nothing called it before `GET /games/levels` did) threw `Cannot set property query of #<IncomingMessage> which has only a getter`. Express 5 made `req.query` a getter-only accessor; the middleware still tried a plain reassignment. Fixed by redefining the property for the query case (`Object.defineProperty`) instead of assigning it. `req.body`/`req.params` are unaffected — they're still plain writable properties in Express 5.
  - **Also fixed a pre-existing stars bug** in the original `/games/quiz/answers` flow noticed while reading `gameService.ts`: it called `calculateStars(isCorrect ? 1 : 0)`, but `calculateStars` is a *cumulative correct-count* threshold function (≥3/≥6/≥10 → 1/2/3 stars) — passing it a single 0-or-1 value always returned 0, so quiz answers never actually awarded stars. Changed to `isCorrect ? 1 : 0` directly, matching how memory verse already did it.
  - Verified end-to-end against the live backend, not just compiled: created a real player, listed levels (level 1 unlocked, rest locked), started a Genesis session, used 2 hints on the first question (confirmed a 3rd is rejected), submitted answers for all 25 questions (correct answer after 2 hints scored exactly 6 points as designed), and confirmed the response reported `isComplete: true` with `nextLevelSlug: "exodus"` — then re-fetched the level list and confirmed Genesis shows completed and Exodus shows unlocked, with levels 3-8 still locked. Also verified the frontend renders this live (score/question-counter/points-cap/lettered-options/hint-button all present in the real DOM, zero console errors) via the browser-automation skill.
  - Design inspiration: the user shared "Brain Test 4" and Lovable-generated mockups mid-session. Adopted the *patterns* (numbered level nodes with lock states, in-quiz HUD, lettered options, a hint button showing its point cost) rebuilt in the app's existing Tailwind/color-token system — did not use the literal images (received as pasted pictures, not asset files) and skipped the marketing-landing-page mockups as out of scope for an in-app product.

- **Redesigned the home page as a full landing page** and gave it a distinct visual identity:
  - Added `royal` (deep indigo/purple) and `gold` (amber) color scales to `tailwind.config.ts` alongside the existing `sunrise`/`sky`/`meadow` palette, plus a `landing-hero` gradient and a `shadow-glow` utility. Did not touch the existing colors, so the game pages (`/quiz`, `/memory-verse`, `/levels/*`) look exactly as before — this was scoped to the landing page only.
  - Rebuilt `Hero.tsx` with the new dark royal/gold gradient hero, real stat callouts, and CTAs into `/levels` and `/memory-verse`.
  - New components: `FeatureGrid` (why kids/grown-ups keep playing), `HowItWorks` (4-step journey), `LevelSystemPreview` (visual explainer of the 8-level path, scoring, and unlock rule, linking to `/levels`), `AudienceHighlights` (families/churches/schools — see honesty note below), `FAQ` (real accordion, `useState`-driven), `Footer` (real nav links only).
  - Extended `GameCard` with an optional `comingSoon` prop (renders a non-clickable card with a "Coming soon" badge instead of a link) rather than creating a duplicate component.
  - Expanded the game showcase to all 10 games from the platform spec (Bible Quiz, Memory Verse, Word Search, Scripture Puzzle, Flash Cards, Match the Verse, Bible Trivia, Bible Story Challenge, Character Guessing Game, Daily Bible Challenge) — only the two that are actually built and playable are marked live; the other eight are honestly marked "Coming soon," not presented as playable.
  - **Deliberately did not fabricate testimonials.** The mockups the user shared included named people, churches, and a "trusted by 50,000+ learners" stat — none of that is real; the app has no real users yet. Built `AudienceHighlights` instead, describing who the platform is designed for (families/churches/schools) without inventing fake reviewers, quotes, or usage numbers.
  - **Caught and fixed two copy claims that weren't true yet** while writing landing page content: "every question comes with an explanation and verse" wasn't actually shown in the UI (the backend already returns `explanation`/`scriptureReference`, nothing rendered it) — added it to both `LevelQuiz.tsx` and `QuizPreview.tsx` so the claim is now true. "Revisit past questions any time" isn't built at all — reworded that line instead of shipping a false claim; a real question-history/review feature is not implemented anywhere yet.
  - Found and fixed a real bug while iterating: the new `bg-landing-hero`/`royal`/`gold` Tailwind utilities didn't apply after editing `tailwind.config.ts` while the dev server was already running — hero rendered with invisible white-on-white text. Fixed by killing the dev server, clearing `.next` entirely, and restarting (a plain hot-reload wasn't enough for this particular config change). Confirmed via screenshot before and after.
  - Verified with a full production build (clean) and a real screenshot of the entire rendered page — zero console errors, zero failed requests, all existing routes (`/`, `/quiz`, `/memory-verse`, `/levels`, `/levels/genesis`) still return 200 after the changes.

## Completed Work — All 10 Games (this pass)
Built and verified end-to-end (real API calls, not just compiled) for every new game:

- **Schema**: added `WordSearchPuzzle`, `BibleCharacter`, `BibleStory` + `StoryEvent`, `DailyChallengeCompletion`. Migration generated via `prisma migrate diff` + `migrate deploy` (non-interactive) and applied cleanly. Extended the existing `importContent` pipeline (`adminService.ts`) to also load `wordSearchPuzzles`, `characters`, and `stories` — so all new content types go through the same admin import path as everything else, not a one-off script.
- **Shared reward logic extracted** into `services/rewardService.ts` (`applyPlayerReward`, `awardProgressRewards`, `logProgress`) so the 6 new game services don't each re-implement the XP/level/streak/badge/avatar-unlock logic that was previously duplicated between `gameService.ts` and `levelService.ts`.
- **Bible Trivia**: reuses the existing quiz session infrastructure entirely (no new backend) — 15 cross-category questions, 15-second per-question timer, auto-submits on timeout. New `TriviaGame.tsx` + `/trivia`.
- **Match the Verse**: memory-match game pairing references with verse text. New `verseMatchService.ts` + `/match-the-verse`.
- **Flash Cards**: self-graded verse recall ("I knew it" / "Still learning"). New `flashCardService.ts` + `/flashcards`.
- **Scripture Puzzle**: tap-to-reorder scrambled verse words, 2 hints max (-2 pts each, reveals the next word in position). New `scripturePuzzleService.ts` + `/scripture-puzzle`.
- **Character Guessing Game ("Who Am I?")**: progressive clues, free-text guess, 2 hints max. New `characterGuessService.ts` + `/who-am-i`.
- **Bible Story Challenge**: reorder story events with up/down controls, exact-sequence check. New `storyOrderService.ts` + `/story-challenge`.
- **Word Search**: real grid-generation algorithm (8 directions, backtracking placement, up to 200 attempts/word), server-side placement stored in session metadata so found-word claims are verified against the actual generated grid, not trusted from the client. Tap-start/tap-end cell selection (accepts either direction). New `wordSearchService.ts` + `/word-search`. Found words are highlighted with an SVG line drawn through the cells (viewBox-based so it scales correctly without DOM measurement) — a different color per word, mimicking marking a find with a pen, per the user's request.
- **Daily Bible Challenge**: one question per calendar day (deterministic date-hash pick from the 200-question pool, same question for every player that day), one completion per player per day enforced by a DB unique constraint. New `dailyChallengeService.ts` + `/daily-challenge`.
- **KJV accuracy pass**: the user explicitly required exact, unparaphrased King James Version text throughout. Audited all 21 authored verses against real KJV wording — found and fixed several that were silently truncated (missing the back half of the verse) and one pre-existing verse (`john-316`, from before this session) that was NIV wording, not KJV. All 21 active verses in the database are now confirmed `translation: "KJV"` via a direct query, zero non-KJV verses remain.
- **20-level/round escalation**, applied everywhere it makes sense (matching what was done for Word Search first, then generalized to the others per the user's request — Bible Quiz Levels was excluded since the user built that separately, and Daily Challenge was excluded since "levels" don't fit a once-a-day mechanic):
  - Word Search: expanded 8 → 20 puzzles (increasing grid size / word length / theme obscurity), serves the lowest-`sortOrder` puzzle this player hasn't completed yet, falling back to random once everything's been played.
  - Scripture Puzzle: same pattern, ordered by verse length (shorter = easier) instead of an authored `sortOrder`, since the existing 21 KJV verses already cover a good difficulty spread — no new content needed.
  - Character Guessing Game: expanded 10 → 20 characters (added Solomon, Abraham, Joseph, Elijah, Gideon, Deborah, Nehemiah, Job, Jonathan, John the Baptist), served in `sortOrder`, skipping characters already guessed correctly.
  - Bible Story Challenge: expanded 5 → 20 stories / 102 total events (added Tower of Babel, Abraham and Isaac, Joseph and His Brothers, The Ten Plagues, Crossing the Red Sea, Battle of Jericho, Samuel Anoints David, Elijah and the Prophets of Baal, Daniel in the Lions' Den, Jonah and the Great Fish, Esther Saves Her People, The Sermon on the Mount, Feeding of the 5,000, The Prodigal Son, Paul's Conversion), same skip-if-completed pattern.
  - Match the Verse & Flash Cards: no new content needed (the 21-verse pool already supports this) — added a level number derived from how many rounds this player has completed, escalating pair-count/deck-size and skewing toward longer (harder) verses as level increases, capped at 20.
  - Bible Trivia: round number tracked client-side (`lib/player.ts`, same pattern as player identity) since Trivia has no single-puzzle identity to track server-side; maps rounds 1–7/8–14/15–20 to the existing easy/medium/hard `difficultySlug` filter the quiz endpoint already supported.
- Landing page game grid and footer updated: all 10 games now link to real, live pages (no more "Coming soon" badges).
- Full regression pass: all 13 frontend routes return 200, zero browser console errors on `/`, `/word-search`, `/trivia` (screenshot + automated checks), both production builds (`tsc` + `next build`) clean.
- **Trophy completion screen** added per the user's request (referencing a "LEVEL X COMPLETE" mockup): new shared `LevelCompleteScreen.tsx` (trophy icon, "You passed!", 0–3 star rating, level score / XP earned / cumulative total score stat pills, a styled "Continue" CTA) used by all 8 games with a level/round concept — Bible Quiz Levels, Word Search, Scripture Puzzle, Character Guess, Story Challenge, Match the Verse, Flash Cards, and Trivia. The "total score" stat is the player's real cumulative `xp` from the backend response, not a per-round number, so it genuinely adds up across levels the way the user asked. Bible Quiz Levels routes to the specific next level's URL; the other 7 (no discrete per-level URL) reload to fetch the next round in sequence. Verified every completion endpoint actually returns `player.xp` at the response path the new frontend code reads, via direct API calls against the live backend for all 6 new games, not just by reading the code.

## Completed Work — Game Selection & Onboarding Flow (this pass)
Reworked how a visitor gets from the landing page into an actual game, per the user's request after seeing screenshots of the old Hero (which jumped straight into Bible Quiz specifics before anyone had chosen what to play):

- **`Hero.tsx` simplified** to a single centered column with one generic "Start Playing →" CTA (previously two specific-game CTAs plus a stat row and two per-game preview cards, all removed). The button now links to `/games` instead of scrolling the same page.
- **New `/games` page**: a dedicated "Choose Your Game" grid listing all 10 games, replacing the games grid that used to live inline on the landing page.
- **New `/games/[slug]` gate page**: sits between "pick a game" and "play the game."
  - Returning guest (already has a stored nickname/player id) — redirects straight through to the real game page, no extra step.
  - First-time guest — shows `PlayerAvatarSetup.tsx`: pick a Boy/Girl avatar, type a nickname, submit (button or Enter). On success, a short celebration screen ("Hi, {nickname}! 🎉") plays a bounce animation on the chosen avatar, then auto-redirects into the real game after ~1.4s.
- **New shared `lib/games.ts`**: single source of truth for the 10 games' slug/title/description/href/icon, read by `/games`, `Footer.tsx`, and (previously) the landing page — replaces three separate copies of the same game list.
- **Removed** the now-redundant `PlayerSetupForm.tsx` (landing-page player creation form) — player setup now happens contextually right after choosing a game, not as a separate, easy-to-miss form at the bottom of the landing page.
- `Footer.tsx` and `LevelSystemPreview.tsx` game links updated to route through `/games/[slug]` instead of the bare game URL, so a first-time visitor arriving from either still gets the nickname/avatar step.
- Verified end-to-end with the browser-automation skill (not just compiled): cleared localStorage, clicked Hero CTA → `/games`, picked Word Search → `/games/word-search` setup gate, picked an avatar, submitted a unique nickname, confirmed the celebration screen showed the right name, confirmed auto-redirect landed on the real `/word-search` page, confirmed `play.nickname`/`play.playerId` were persisted, then confirmed a second visit to `/games/levels` (same "browser") skipped setup and redirected straight to `/levels`. Full production build (`next build`) clean; `/games` is static, `/games/[slug]` is dynamic (correct, since it reads `localStorage`).

## Roadmap: Mobile App Store Launch (in progress)
User wants Play on the Google Play Store and Apple App Store, downloadable as a real app. Planned together in chat. Decisions locked in:
- **Audience framing: family app**, not a strict "kids' app." Standard app store review applies (still needs a privacy policy and honest data handling), not Apple's Kids Category or Google's Designed for Families program — avoids the heaviest COPPA/parental-consent/no-third-party-tracking constraints those trigger.
- **Packaging: Capacitor**, not a native rewrite. Wraps the existing Next.js frontend into real iOS/Android app shells, reusing everything already built. Chosen because Play is tap/quiz-driven, not graphics-intensive, so a WebView-based shell is a legitimate fit, not a compromise.
- **iOS builds: cloud Mac build service** (Codemagic), since development is on Windows with no Mac available. Signing/compiling for the App Store happens in the cloud, not locally.

### Architecture decision (revised): hybrid strategy, confirmed by the user after hitting a real budget constraint
Apple's $99/year and Google's $25 one-time fee are both real costs with no waiver available to an individual (Apple's fee waiver exists but only for verified nonprofits/schools, not applicable here). Rather than block on that, the user confirmed the following as the actual architecture going forward — this doesn't undo any work already done, it's exactly the direction Phase 3 already happened to take:
- **The Next.js web app is the primary platform.** It must work well on Android, iPhone, tablets, and desktop browsers via the browser directly — not just as a wrapped native shell. Verified in this pass: responsive at tablet (768px) and desktop (1440px) widths with real screenshots, not just "no horizontal scrollbar." Tried a `lg:grid-cols-3` desktop breakpoint on the games grid to use more desktop width, but the user corrected this back to a fixed 2-column grid (`sm:grid-cols-2`, no `lg:` override) — 10 games in 3 columns gives an uneven 3-3-3-1 split; 2 columns gives a clean 2-2-2-2-2, which the user considers the better design regardless of unused desktop space. Left at `max-w-7xl` (widened from the original `max-w-6xl` to match the rest of the site).
- **The web app is also a installable PWA with offline support**, built in this pass:
  - `public/manifest.webmanifest` (name, icons at 192/512px generated from the real logo, `display: standalone`, theme color matching the brand).
  - `public/sw.js`, a hand-written service worker (deliberately not a plugin like `next-pwa`, to keep full control and avoid interactions with the conditional static-export config the mobile build already depends on): cache-first for Next's content-hashed static assets, network-first-with-cache-fallback for page navigations, and a strict pass-through for anything hitting the API's different origin (game data must never be served stale).
  - `components/ServiceWorkerRegistration.tsx` registers it, production-only (registering in dev would fight Next's own hot-reload caching).
  - Wired into `app/layout.tsx` via Next's native `Metadata.manifest` field and a `Viewport` export for `themeColor` — no extra dependency needed for the manifest link itself.
  - **Verified working, not just configured**: real production build, real production server, confirmed the manifest resolves (200, correct name/icons), the service worker actually registers, and — the real test — visited a page, went fully offline (`context.setOffline(true)`), reloaded, and the cached page loaded correctly with zero errors.
- **Android: the same Next.js app packaged into an APK via Capacitor** — this was already the Phase 3 approach, now explicitly confirmed as the long-term one rather than a stopgap. No separate React Native or Flutter codebase, and none is planned.
- **iOS: architecture stays ready for it** (the Capacitor `ios/` project already exists and already produced one successful Codemagic build), but actually shipping it is paused on the same $99/year Apple constraint — not a structural blocker, purely a "revisit when there's budget" item.
- **Backend unchanged**: Express + Prisma + PostgreSQL, single codebase, no duplicated business logic — this was already true and remains true; the hybrid decision doesn't touch the backend at all.
- **Distribution channel — decided: direct APK download, hosted on the website itself. DONE, live.** The user confirmed the budget constraint is real (not solvable by a nonprofit-style Apple fee waiver, which doesn't apply to an individual) and explicitly deprioritized official Apple/Google store submission entirely, not just "paused for now" — so Phase 4/5's store-submission track (developer accounts, TestFlight, Play Store review) is no longer being pursued. Everything already built for it (Privacy Policy, Terms, signed Android release, store listing copy, screenshots) stays in the repo untouched since it's harmless to keep and cheap to revisit later, but it's not active work.
  - Built a real signed **installable release APK** (not the AAB — that's Play Store's upload-only bundle format, not directly installable): `./gradlew assembleRelease`, verified with `apksigner verify` (real signature check, not just "the build didn't error") before treating it as done.
  - Hosted at `frontend/public/downloads/play.apk`, served through the same Render deploy already working — confirmed the production server returns the correct `Content-Type: application/vnd.android.package-archive` and matching `Content-Length`, not just a 200.
  - New `/download` page: a clear "Download for Android" button plus step-by-step sideload instructions (Android will warn about an unrecognized source since this isn't the Play Store — walked through exactly how to allow it), with an honest note that the web app itself works great without installing anything. Linked from the Footer and from the landing page's game-selection section.
  - iOS stays exactly where it was (Capacitor project + one successful Codemagic build) — not removed, not being actively pursued either.

### Landing page: "Sign In to Play" messaging (Hero + main CTAs), guest play unchanged underneath
The user asked to make the account Sign In link more prominent (it was only in the Footer and easy to miss) and to surface the Android download at the top of the page too. Along the way there was a real back-and-forth worth recording precisely, since it involves a change that was tried and then explicitly reverted:
- **First attempt (reverted): made sign-in functionally required to play.** Interpreted an ambiguous voice-dictated message as "require an account before playing" and implemented it for real — changed `GameSetupClient.tsx` to redirect to `/account` instead of allowing instant guest setup, and deleted `PlayerAvatarSetup.tsx` as unused. The user caught this immediately ("keep my app the way I have designed it... I only told you to push the sign in up and make the download for android visible") and it was reverted via `git checkout` of both files back to their last committed state before anything was committed — guest play (no account needed) works exactly as it always has, unchanged.
- **What was actually wanted, confirmed**: purely a landing-page messaging/navigation change, not a functional gate.
  - `Hero.tsx`: primary CTA relabeled from "Start Playing →" to **"Sign In to Play →"**, now linking to `/account` instead of `/games`. A second, visually secondary "⬇ Download for Android" link sits directly underneath it (stacked, not side-by-side) linking to `/download`.
  - The two other landing-page CTA buttons ("See All Games →" and the bottom section's "Start Playing →") were relabeled to the same **"Sign In to Play →"** and repointed to `/account`, for consistency — the user flagged both by screenshot one at a time and asked for the same treatment as the Hero.
  - `/download` page's "play in your browser instead" link also repointed from `/games` to `/account` for the same consistency.
  - **What this does *not* change**: the games list (`/games`) is still directly reachable by URL and still fully browsable; `/games/[slug]` still lets a first-time guest set up a nickname/avatar and play immediately with no account, exactly as before. Only the landing page's own wording/links changed to lead with signing in — the underlying app behavior the user designed is untouched.

Phases, in order (each phase blocks the next):

1. **Production infrastructure — DONE. Live on Render.**
   - `play-db` (Postgres 17), `play-api` (Express/Prisma backend), `play-frontend` (Next.js) all deployed via the `render.yaml` Blueprint. Live URLs: backend `https://play-api-hb7f.onrender.com`, frontend `https://play-frontend-cz51.onrender.com` (both `play-api` and `play-frontend` names were already taken on Render, so each got a suffix — `render.yaml` reflects the real assigned URLs, not the originally-guessed clean ones).
   - Real bugs hit and fixed along the way, all now in `main`:
     - **Missing build tools on Render**: `render.yaml` sets `NODE_ENV=production`, and `npm install` skips `devDependencies` under that env var (verified empirically). `tailwindcss`/`postcss`/`autoprefixer`/`typescript`/`@types/*` were all devDependencies on both projects, so none of them installed, breaking both builds. Fixed by moving everything actually needed to build into `dependencies` on both `frontend/package.json` and `server/package.json` (backend's `tsx` stayed a devDependency — only used for local dev/scripts, not the production path).
     - **Backend crashed on boot** (`ERR_MODULE_NOT_FOUND` on `dist/app`): compiled to native ES modules, but the source's relative imports don't include `.js` extensions, which Node's strict ESM loader requires (worked locally only because `tsx` is more permissive than plain `node`). Fixed by compiling to CommonJS instead (`server/tsconfig.json`: `module: CommonJS`, `moduleResolution: Node10`; removed `type: module` from `server/package.json`) — zero source changes needed since no ESM-only features exist anywhere in `server/src`.
     - **Bulk content imports timing out against the real database**: Prisma's default interactive-transaction timeout is 5s; importing ~250 records with related child rows over a real network connection (not local Docker) blew straight through it. Raised to 180s/20s (timeout/maxWait) in `adminService.ts`'s `importContent` transaction.
   - Production database seeded for real: ran `seed.ts`, `importLevelsContent.ts`, and `importExtraGamesContent.ts` locally against the production `DATABASE_URL` (Render's *external* connection string, used once and not stored anywhere in the repo) — 8 categories, 3 difficulties, 200 leveled quiz questions, 21 KJV verses, 20 word search puzzles, 20 characters, 20 stories, all confirmed present in production.
   - Verified with a full browser-driven flow against the live URLs, not just spot checks: cleared state, picked a game, created a real player against the real backend, landed on the real `/word-search` page, and confirmed a real puzzle ("Creation Week", a real 12×12 grid) rendered from the production database.
   - `ADMIN_BOOTSTRAP_EMAIL`/`ADMIN_BOOTSTRAP_PASSWORD` still unset in Render's dashboard (optional — only needed for a real admin login; left as `sync: false` in `render.yaml` so nothing gets committed).
   - Known limitations, unchanged from the original plan: Render's free Postgres expires after 90 days and needs manual renewal; free-tier services spin down after inactivity, so the first request after idle takes 50+ seconds (confirmed live — a cold request took ~60s, subsequent ones were fast). Neither is a bug, both are free-tier tradeoffs to revisit before a real public launch.

2. **Login/accounts — DONE (email/password only, guest play still works unchanged).** Decisions locked in with the user before building: email/password only for now (no Google/Apple OAuth yet — avoids the Apple rule that requires Sign in with Apple as an equal option the moment you offer any other third-party login, deferred until closer to actual store submission); creating an account is optional, not required to play.
   - **Schema**: `ParentAccount` (email, passwordHash, isActive) + `ParentSession` (hashed bearer token, expiresAt) — deliberately mirrors the existing `AdminUser`/`AdminSession` pattern exactly (same `scryptSync` password hashing in `utils/password.ts`, same SHA-256-hashed-token session pattern in `utils/token.ts`) rather than inventing a second auth style. `PlayerProfile` gained a nullable `parentAccountId` — nullable so every guest player created before this shipped, and every guest player who never signs up, keeps working exactly as before.
   - **Backend**: `parentAuthService.ts`, `parentAuthController.ts`, `parentAuthRoutes.ts`, `parentAuthValidators.ts`, and an `authenticateParent` middleware (mirrors `authenticateAdmin`) — mounted at `/api/parents`. `POST /signup`, `POST /login`, `GET /me` (lists child profiles), `POST /children` (add a new child profile), `POST /children/claim` (link an *existing* guest `PlayerProfile` to this account instead of creating a new one).
   - **Guest-progress migration**: signing up optionally accepts `claimPlayerId`; the frontend automatically passes the browser's current `play.playerId` (if any) when someone signs up, so an existing guest's XP/streaks/progress get linked to the new account instead of abandoned. Claiming is idempotent (same parent claiming twice is a no-op) and rejects claiming a profile another account already claimed (409).
   - **Frontend**: `lib/parentAuth.ts` (token storage, same localStorage pattern as `lib/player.ts`), `lib/avatars.ts` (extracted shared avatar list, was previously duplicated), new `/account` page (login/signup toggle, then a dashboard listing child profiles with a "Play" button per child and an "+ Add a Player" form). `PlayerAvatarSetup.tsx` (the first-time guest setup gate) now links to `/account` for anyone who already has a family account. `Footer.tsx` links to `/account` too.
   - "Play" on a child profile sets `play.playerId`/`play.nickname` to that child and routes to `/games` — from there, every existing game page and the `/games/[slug]` setup gate work completely unchanged, since a claimed/parent-owned player is still just a normal `PlayerProfile` row to all existing gameplay code.
   - Verified end-to-end against the real local backend, not just compiled: signup, login, `/me`, add-child, claim-guest-profile, duplicate-email rejection (409), wrong-password rejection (401), cross-account claim rejection (409) all tested via direct API calls; then the full browser flow (guest plays → signs up → guest profile auto-claimed → visible on dashboard → add a second child → sign out clears session and resets to the login view → sign back in → both children still there → Play sets the right identity and lands in `/games`) verified via the browser-automation skill.
   - **Real bug found and fixed via that browser test**: signing out didn't reset `authMode` back to `"login"`, so a parent who'd been on the signup tab and then signed out would see the signup form again instead of a login form — fixed in `handleSignOut`.
   - Migration `20260806190449_add_parent_accounts` generated and applied locally; will apply to production automatically on next deploy via `render-start`'s `prisma migrate deploy` step, same as every migration before it.
   - Not yet done: deploying this to Render (still needs `git push` + confirming the migration ran in production, same verification pattern as Phase 1). Google/Apple sign-in intentionally deferred, not forgotten — revisit before actual store submission if desired.

3. **Mobile packaging (Capacitor) — DONE.** Decisions locked in with the user before building: bundle a static export inside the app (not a thin WebView pointing at the hosted site) for a stronger app-review case and offline-capable UI shell; cloud builds for **both** platforms, not just iOS — this environment turned out to have no Java/Android SDK/Gradle installed either, so "Android builds locally on Windows" from the original plan wasn't actually available. App identifier: `com.playbible.app`, display name "Play".
   - **Website untouched.** `next.config.mjs` only switches to `output: "export"` when `CAPACITOR_BUILD=1` is set — the Render deployment's normal `next build`/`next start` path is completely unaffected, verified by rebuilding it after all these changes and confirming it still produces a normal `.next` server build (not a static export).
   - **Static export required real route changes**, not just a config flag: `/games/[slug]` and `/levels/[slug]` are dynamic segments, and static export needs `generateStaticParams()` for every one, which can't coexist with a `"use client"` page file. Split `/games/[slug]/page.tsx` into a server component (exports `generateStaticParams`, lists all 10 slugs from `lib/games.ts`) rendering a new `components/GameSetupClient.tsx` (the original client logic, unchanged). `/levels/[slug]/page.tsx` was already a server component, so it just gained `generateStaticParams()` reading a new `lib/levels.ts` (the 8 category slugs, mirrored from `levels-content.json` since the frontend didn't have a static list before). **Side benefit**: both routes now pre-render as static HTML on the website too, upgraded from `ƒ Dynamic` (server-rendered every request) to `● SSG` (built once) — a free performance win, not just mobile groundwork.
   - `npm run build:mobile` (new script, uses `cross-env` for a Windows-safe env var) builds the static export with `NEXT_PUBLIC_API_BASE_URL` pointed at the **production** API (`play-api-hb7f.onrender.com`), not localhost — the packaged app has no access to a dev machine. `npm run cap:sync` runs that build then `npx cap sync` to copy the output into both native projects.
   - Verified the static export actually works, not just that it builds: served `out/` locally and ran a full browser test — client-side navigation (Hero → `/games` → pick a game → setup gate), a real signup-style flow, and a real word search puzzle ("Creation Week") loaded from the live production API, all through pure client-side routing with zero console errors.
   - `capacitor.config.ts` added; `npx cap add android` and `npx cap add ios` both succeeded without needing local Java/Xcode (they just scaffold the native project from a template — only *building* those projects needs the real toolchain, which is why this pass didn't attempt a local build).
   - App icon and splash screens generated via `@capacitor/assets` from the real logo (`frontend/resources/icon.png`, composited onto the app's royal-purple brand color since app icons need a filled background, not the transparent web version) — 100 Android assets (all density buckets, light/dark adaptive icons) and 13 iOS assets (all required sizes, light/dark splash) generated automatically, not hand-sized. A stray PWA icon set/manifest the generator also produced was removed since this isn't a PWA and nothing referenced them.
   - `codemagic.yaml` added at the repo root: an Android workflow (unsigned debug APK, installable for testing right away, no Google Play Console account needed yet) and an iOS Simulator workflow (no Apple Developer Program enrollment needed for a simulator build). Real release-signed builds for actual store submission need signing credentials added in Codemagic's dashboard once the developer accounts from Phase 4 exist — documented in the yaml's comments, not something Claude can configure without those accounts.
   - `.gitignore` extended for native build artifacts (`android/app/build`, `android/.gradle`, `ios/App/Pods`, etc.) — the native project *source* is committed, generated build output is not, same convention as `node_modules`/`.next`.
   - **Revised after this pass**: the user decided against Codemagic for Android specifically — installed Android Studio locally instead, so Android builds happen on their own machine going forward. `codemagic.yaml` trimmed to iOS only (still needed; there's no way around requiring some Mac-based build for iOS without an actual Mac).
   - **Local Android build — DONE, verified with a real APK, not just configured.** The user installed Android Studio and completed its SDK setup wizard (installed to the default `%LOCALAPPDATA%\Android\Sdk`).
     - Hit a real build failure getting there: Android Studio's bundled JDK is version 25, too new for Gradle 8.14.3 (`Unsupported class file major version 69`). Gradle's own DNS resolution also failed on the first real attempt (`No such host is known` for `dl.google.com`/`repo.maven.apache.org`) despite the network genuinely being reachable (verified via both `curl` and a direct Java `InetAddress` test) — resolved by killing the stale Gradle daemon and retrying, so it was transient, not a real config problem.
     - Fixed the JDK issue by downloading a portable Eclipse Temurin JDK 21 (LTS, Gradle-compatible) to `C:\Users\<user>\.android-build-tools\` — a machine-local tool, not part of the repo. Created `frontend/android/local.properties` (gitignored, machine-specific) pointing at the installed SDK.
     - To rebuild: from `frontend/android`, with `JAVA_HOME` set to the Temurin 21 path and `ANDROID_HOME` set to the SDK path, run `./gradlew assembleDebug`. First run took ~17 minutes (downloading the Android Gradle Plugin and every transitive dependency); should be much faster on subsequent runs since Gradle caches them.
     - **Real APK built and verified**: `frontend/android/app/build/outputs/apk/debug/app-debug.apk` (~8MB). Inspected with `aapt dump badging`, not just checked for existing: package `com.playbible.app`, label "Play", `INTERNET` permission present (needed for API calls), targets SDK 36.
     - Not yet done: actually installing this APK on a real device or emulator to see the app run (no device was connected during this pass) — via `adb install app-debug.apk` with a device connected over USB (debugging enabled), or by copying the APK to a phone directly and enabling "install from unknown sources".
   - **Codemagic iOS build — DONE, verified with a real successful build, not just configured.** The user connected the repo to Codemagic. Real setup friction hit and fixed along the way, each confirmed by an actual build log, not guessed at:
     - Codemagic's project auto-scanner didn't find a mobile project at the repo root (it's nested under `frontend/` in this monorepo) — fixed by setting the Project Path field to `frontend` in Codemagic's UI.
     - Build failure: `The Capacitor CLI requires NodeJS >=22.0.0` — `codemagic.yaml` had `node: 20`; bumped to `node: 22`.
     - Build failure: `No 'Podfile' found in the project directory` — this project uses Capacitor's Swift Package Manager integration (`ios/App/CapApp-SPM`), not CocoaPods (Capacitor 8 defaults to SPM), so there's no Podfile and no `.xcworkspace`. Removed the `pod install` step and pointed `xcodebuild` at `App.xcodeproj` directly.
     - **Result**: a clean successful build (`Status: finished`, all steps green, ~2m 23s) — confirmed via the Codemagic build overview, not assumed. Both platforms are now verified building for real: Android locally (a real installable APK), iOS on Codemagic (a real Simulator build).
   - Not yet done on either platform: actually running the built app (installing the APK on a device, or running the Simulator `.app` artifact) to see the packaged app work end-to-end on-device. Both platforms' store-ready *release* (not debug/simulator) builds, with real signing, wait on Phase 4 (developer program enrollment + signing setup).

4. **Store compliance & submission prep — everything Claude can do without the user's paid accounts is done.**
   - **Privacy Policy** (`/privacy`) and **Terms of Service** (`/terms`) — real pages, live on the site, linked from the Footer. Content grounded in what the app actually does (verified by grepping the codebase for analytics/trackers/ads before writing a word, not assumed): no ads, no third-party analytics anywhere, guest play needs zero personal info, accounts collect only an email + a hashed password, child profiles never collect a child's own personal information. Contact email (`findurgently@gmail.com`, provided by the user) used throughout — flagged clearly to the user first that some real contact method is a hard requirement for both stores' developer consoles, not a stylistic choice, before asking for it.
   - Swept both new pages for "family" wording to match the earlier sweep across the rest of the app — caught a few instances that slipped into the first draft and fixed them before shipping.
   - **Android release signing — DONE, verified with a real signed build.** Generated a real keystore (`keytool`, RSA 2048, 10000-day validity, random password via Python's `secrets` module) and wired `signingConfigs.release` in `build.gradle` to read credentials from `keystore.properties` (gitignored, never committed — losing this file or its password would compromise the ability to publish updates under this app's identity later, so it must be backed up somewhere safe outside the repo, not just on this machine). Hit one real bug along the way: a path mismatch (`keystore.properties` pointed at the keystore file without the `app/` prefix it actually needed) caused `validateSigningRelease` to fail with "Keystore file not found" on the first attempt — fixed, then verified with a full `./gradlew bundleRelease` run producing a real ~5.5MB signed `app-release.aab` (Google Play's required upload format for new apps).
   - **Store listing content drafted** at `store-listing.md` (repo root): app name, short/full descriptions, keywords, category, content-rating prep notes (answers ready for the actual questionnaires once there's dashboard access) — all grounded in real app behavior, nothing aspirational.
   - **Real screenshots captured**, not placeholders: `store-assets/screenshots/` — landing page, the full 10-game list, the level map, and live gameplay (a real word search puzzle loaded from the production database), captured against the actual production site at iPhone-class portrait resolution (1170×2532) using the browser-automation skill.
   - App icons at all required sizes — already done in Phase 3 (`@capacitor/assets` generation).
   - **What's left, and genuinely needs the user** (payment + identity verification Claude has no way to provide): Apple Developer Program enrollment ($99/yr), Google Play Console enrollment ($25 one-time). Once those exist, the actual content-rating questionnaires get filled out in each dashboard (answers already prepared in `store-listing.md`), and iOS release signing (needs an Apple-issued certificate, which needs the Apple Developer account first).

5. **Review & launch — blocked on Phase 4's developer accounts, nothing further to build until then.**
   - TestFlight (iOS) and Internal Testing track (Android) first, then public release.
   - Once the developer accounts exist: create the app listing in each console using `store-listing.md`'s content and `store-assets/screenshots/`, upload the release `.aab` (Android, already built) and a properly signed `.ipa` (iOS, needs Apple signing set up first), fill out the content rating questionnaires, submit for review.

## Completed Work — Snake-Path Level Maps + Milestone Rewards (this pass)
Every game with a level/round concept now shows a Duolingo/Candy-Crush-style winding level map after picking the game, before actual play — sequential lock/unlock, a highlighted "Start" node, replayable completed nodes where the game supports it. Previously only Bible Quiz Levels had any level-selection step at all, and even that was a plain 2-column card grid, not an actual path.
- **New generic `PlayerContentProgress` model + `contentProgressService.ts`** (`listLevels`/`isContentUnlocked`/`markCompleteAndUnlockNext`), reused by the four "collection" games (Word Search, Scripture Puzzle, Character Guess, Story Challenge) instead of four near-duplicate progress trackers — mirrors the existing `PlayerLevelProgress` pattern from `levelService.ts`. Each of those 4 services gained a specific-level start param, unlock enforcement, and a `list*Levels()` export, plus new `GET .../levels` endpoints.
- **Character Guess previously only supported starting a batch of 5 characters per session** — added a single-character mode for level play without touching the old batch behavior.
- **Match the Verse / Flash Cards** already derived a server-side level from completed-session counts; exposed via new `GET .../level-map` endpoints. **Trivia previously only tracked its round in `localStorage`** (incrementing on every page load, not every completion, a real pre-existing bug) — now server-authoritative via a `metadata.mode: "trivia"` tag on the shared quiz session (`gameMode` stays `"quiz"`, so Quick Practice is unaffected — verified live that the two don't cross-contaminate each other's counts).
- **New `SnakeLevelMap` frontend component** — the actual winding-path visual, previously nonexistent anywhere in the app — reused via two thin wrappers: `ContentLevelMap` (collection games, full level list) and `ProceduralLevelMap` (Match the Verse/Flash Cards/Trivia, which have no discrete per-level content, so only the current position is ever clickable). Each collection game's top-level route now serves the map; gameplay moved to `/<game>/[levelSlug]` (mirroring the existing `/levels` + `/levels/[slug]` split). Procedural games moved gameplay to `/<game>/play`. `/levels` itself swapped from the plain card grid to the same visual, for consistency across all 8 games.
- ~~"Level N/20" mid-game HUD badge missing on 4 games~~ — **done**, added to Word Search/Scripture Puzzle/Character Guess/Story Challenge's headers, matching the pattern already used by Match the Verse/Flash Cards/Trivia.

## Completed Work — Completion-Screen Navigation + Character Guess Content Depth (follow-up pass)
User feedback after the level-map pass: completion screens needed a way back to the map (not just forward to the next level), and some games (Scripture Puzzle, Character Guess, Story Challenge) felt too easy at one item per level compared to Word Search/Bible Quiz Levels.
- **`LevelCompleteScreen` now always shows two actions**: the primary continue (Next Level, or Back to Levels once a game's final level is reached) plus a secondary "Back to Levels" that always returns to that game's map. Wired into all 8 games; Match the Verse/Flash Cards also lost their old `window.location.reload()` in the process, now routing properly.
- **Character Guess reworked from 1 character per level to real per-level effort**: reuses the same 8-category structure Bible Quiz Levels already has (categories with zero characters — Parables, Miracles — are skipped, giving 6 real levels: Genesis, Exodus, Kings, Prophets, Jesus, Apostles). Each level pulls up to 20 characters from its category; a level only completes once every character in it is guessed correctly, not just the last one answered.
- **Character pool expanded from 20 to 70**, sourced partly from a Q&A list the user provided (`question.md`, repo root, not part of the app's data — used as source material only), cross-checked against real Scripture before writing clues. Current per-category counts, disclosed honestly rather than padded: Genesis 8, Exodus 10, Kings 15, Prophets 13, Jesus 10, Apostles 14 — not yet a full 20 everywhere.
- **Verified live**: category-based level list loads with correct lock states, a full 8-character Genesis level solved through real API calls with correct names, level marked complete, Exodus unlocked after.
- **Scripture Puzzle and Story Challenge still have one item per level** — same treatment is the clear next step, flagged rather than silently left half-done. Needs its own content-authoring pass: more KJV verses for Scripture Puzzle, and especially more sequenced-event stories for Story Challenge, which are far more labor-intensive to author accurately than a character's clue set (each needs a full ordered narrative, not just three clues).

## Completed Work — Every Game Toward 20/Level, Scripture Puzzle Done (second follow-up pass)
User set a firm standing target: every level-based game should carry ~20 items per level (matching Bible Quiz Levels' 25/level as the one deliberate exception), content should span all 66 books of the Bible, and KJV wording must be exact.
- **Trivia**: 15 → 20 questions/round. **Match the Verse**: pair-count cap 10 → 20/level. **Flash Cards** already ramps to ~21 (full verse pool) at its top level, left as-is.
- **Word Search**: all 20 puzzles expanded from ~7-10 words to ~20 each using real, verifiable Bible content (book names, kings, cities, prophets, Revelation imagery, etc). Three puzzles honestly left below 20 rather than padded with off-theme filler: Fruit of the Spirit (9 — that's all of Galatians 5:22-23), the Ten Commandments (10 — there are only ten), the Twelve Disciples (11 — only twelve exist, several share a first name). Verified every puzzle's grid generation actually places all of its words (ran the real placement algorithm standalone, 20 trials per puzzle, zero drops) — not just that the grid was nominally big enough.
- **Scripture Puzzle — done.** Verse pool expanded from 21 to 70, adding one verse for every one of the 66 books not yet represented (was 17 books covered, now all 66). **Every new verse's exact wording was checked against bible-api.com's real KJV endpoint before being added**, not typed from memory — getting this wrong would be a real quality failure since players unscramble the literal text. Reworked from single-verse-per-level to the same 8-category structure as Character Guess (6 real levels: Genesis, Exodus, Kings, Prophets, Jesus, Apostles), up to 20 verses per level. This needed more backend work than Character Guess: Scripture Puzzle had no pre-existing multi-item support to restore, so sessions now hold an ordered list of verses with cumulative completion tracking, and the frontend was rewritten to iterate through a level's verses one at a time. Verified live: a full 4-verse Exodus level solved end-to-end through the real API with cumulative correctness tracking, next level unlocked after. Genesis's own category is thin (1 verse) — honest gap, not padded.
- ~~Story Challenge still one item per level~~ — **done, third follow-up pass.** Story pool expanded from 20 to 53, adding 33 new sequenced narratives (Cain and Abel, the Golden Calf, Elijah Taken Up to Heaven, the Transfiguration, Jesus Raises Lazarus, the Good Samaritan, Pentecost, Paul and Silas in Prison, and more) to fill out categories that had only 1-4 stories before. Unlike the verse work, accuracy here is about getting the real *sequence* of events right (narrative summaries, not literal KJV quotes), checked against the actual scriptural account for each story. Reworked to the same category-based leveling as Scripture Puzzle — and since Story Challenge is the only game with content in all 8 categories, it's the first to get a full 8-level map (Genesis, Exodus, Kings, Prophets, Parables, Jesus, Miracles, Apostles) instead of 6. Verified live: solved a full 8-story Genesis level end-to-end through the real API using the actual correct event order from the database, cumulative completion tracked correctly, Exodus unlocked after.
- **This closes out the "every game to ~20 items per level" initiative.** All 8 level-based games now have real, verified, multi-item levels: Bible Quiz Levels (25/level, the original), Word Search, Scripture Puzzle, Character Guess, Story Challenge (up to 20/level each), and Match the Verse/Flash Cards/Trivia (procedurally scaled to the same ~20 ceiling). Full production build clean across all of them.
- **Milestone rewards — real, not stubbed.** Consolidated a pre-existing duplicate: `levelService.ts` had its own separate copy of the badge/avatar-unlock logic instead of using `rewardService.ts`'s shared `awardProgressRewards` like every other game did. Now one implementation, extended with a new `Badge.levelsCompletedThreshold` field checked against total levels completed across `PlayerLevelProgress` + `PlayerContentProgress`. **Discovered zero `Badge` rows existed anywhere** (no seed path had ever existed for them, a genuine pre-existing gap) — added `badges` support to the generic `importContent` pipeline and seeded 4 real milestone badges (5/10/25/50 levels completed) via `server/src/scripts/importMilestoneBadges.ts`, run locally. **Not yet run against production** — needs `npx tsx src/scripts/importMilestoneBadges.ts` run once against the production `DATABASE_URL`, same pattern as the original content seeding.
- **Verified live, not just compiled**: full solve-and-unlock chain proven via real API calls for all 4 collection games (correct lock state before, real puzzle solved through server-side validation, `nextLevelSlug` returned, next node unlocked after); milestone badge confirmed unlocking after really completing 5 levels; Trivia's round isolation confirmed against a real Quick Practice completion; all 8 level-map pages loaded live with zero console errors and zero failed requests; full production build clean.
- Explicitly deferred, not silently skipped: coins/currency, leaderboards, and question-review history — each flagged back to the user as needing its own design conversation (currency has nothing to spend on yet, leaderboards need a privacy decision for a family app with kids, review history needs a new UI) rather than being half-built alongside this.

## Completed Work — Session Resume / Continue Where You Left Off (this pass)
User request: leaving a level partway through and coming back later should **resume** from where they stopped, not force a restart from question 1 — with an explicit opt-in to restart for anyone who wants to. Applies to all 5 games that use a multi-item session (Bible Quiz Levels, Word Search, Scripture Puzzle, Character Guess, Story Challenge); Match the Verse/Flash Cards/Trivia don't need this since they're single-round-per-play, not a persistent in-progress level.
- **New shared `server/src/services/sessionResumeService.ts`**: `findActiveLevelSession` (looks up an existing `ACTIVE` `GameSession` for this player+game+level via a Prisma JSON-path filter on `metadata`), `abandonSession` (marks a session `"ABANDONED"`, a new status value alongside the existing `"ACTIVE"`/`"COMPLETED"`), `answeredItemIds` (the set of question/item ids already answered in a session, read from `GameSessionAnswer` — the real source of truth for "what's left").
- **All 5 services** (`levelService.ts`, `wordSearchService.ts`, `scripturePuzzleService.ts`, `characterGuessService.ts`, `storyOrderService.ts`) now accept an optional `restart` flag on session start. Default (no flag): if an active session already exists for that level, reuse it and return only the still-unanswered items. With `restart: true`: abandon the old session and start clean with the full item set.
- **Word Search needed a different approach** than the other 4 (it doesn't use `GameSessionAnswer` at all — progress lives in `session.metadata.foundWords`/`placements`). Added `rebuildGridFromPlacements()` to regenerate the same grid layout from the stored word placements on resume (filler cells get fresh random letters, harmless since they never spelled anything), and expose `foundPaths: {word, path}[]` so the frontend can redraw the found-word markers — safe to reveal only for words already solved, preserving the anti-cheat property for the rest.
- **Frontend**: each of the 5 game components (`LevelQuiz.tsx`, `WordSearch.tsx`, `ScripturePuzzle.tsx`, `CharacterGuess.tsx`, `StoryChallenge.tsx`) refactored around a `loadSession(restart)` function; progress display ("Question X of Y") now reads the session's true `totalQuestions` and an `answeredOffset` (= totalQuestions − remaining items returned) instead of just the local array length, so it's correct on a resumed session. `sessionScore` (and, for Character Guess, `correctCount`) now initializes from the resumed session's real DB fields instead of 0, so points/star-rating math stays correct across a resume. A "Start over" link appears once there's something to restart from, behind a `confirm()` prompt, calling `loadSession(true)`.
- **Real bug caught while implementing this**: Character Guess's star-rating calc (`correctCount / totalQuestions`) would have been wrong on a resumed session since `correctCount` wasn't being read from the resumed session's DB value — fixed before it shipped.
- **Verified live** for all 5 games via real API calls (not just `tsc`): started a level, answered some items, called start-session again without `restart` and confirmed the same session id, the correct remaining item count, and the correct carried-over score; then called with `restart: true` and confirmed a fresh session with the full item set and score reset to 0. Scripture Puzzle specifically verified on a 12-verse level (not the 1-verse Genesis level, which completes in a single answer and has no "leave partway through" state to test) — same pattern held. Test players cleaned up afterward.
- Both `tsc --noEmit` (backend and frontend) clean.

## Completed Work — Bible Quiz Levels: All 66 Books (this pass)
User request: add the remaining 64 Bible books to Bible Quiz Levels as individual levels (Genesis and Exodus already existed), each book getting "as many good questions as it genuinely supports" rather than padded to a fixed count — approach explicitly approved by the user after a clarifying question about how to handle tiny books. This was the largest single content-authoring pass of the project.
- **64 new `Category` rows added**, one per remaining Bible book, `sortOrder` continuing from 9 through 72 (after the original 8 thematic categories: genesis, exodus, kings, prophets, parables, jesus, miracles, apostles). Book categories sit in canonical Bible order after the thematic ones, so the level map now reads: the original 8 thematic levels, then Leviticus through Revelation in order.
- **756 new quiz questions authored and imported across 9 batches**, each batch its own JSON file (`server/src/data/levels-books-batch1.json` through `batch9.json`) run through the existing `importContent` pipeline via a new reusable `server/src/scripts/importBooksBatch.ts` (takes a file path argument, replacing one-off per-batch scripts):
  1. Leviticus, Numbers, Deuteronomy, Joshua, Judges, Ruth — 101 questions
  2. 1–2 Samuel, 1–2 Kings, 1–2 Chronicles, Ezra, Nehemiah, Esther — 126 questions
  3. Job, Psalms, Proverbs, Ecclesiastes, Song of Solomon — 74 questions
  4. Isaiah, Jeremiah, Lamentations, Ezekiel, Daniel — 65 questions
  5. All 12 Minor Prophets (Hosea–Malachi) — 114 questions (completes the Old Testament)
  6. Matthew, Mark, Luke, John — 62 questions
  7. Acts, Romans, 1–2 Corinthians, Galatians — 54 questions
  8. Ephesians, Philippians, Colossians, 1–2 Thessalonians, 1–2 Timothy, Titus, Philemon — 74 questions
  9. Hebrews, James, 1–2 Peter, 1–3 John, Jude, Revelation — 86 questions (completes the New Testament)
- **Real, honest per-book counts, never padded**: ranges from 5 (Philemon, Paul's shortest letter) up to 22 (Psalms) — most books land between 8 and 20. Every question has a real scripture reference and a one-line explanation, matching the existing `levels-content.json` schema exactly.
- **Deliberately avoided duplicating the 8 existing thematic categories**, which already cover a lot of ground (Jesus' life/miracles/parables, the apostles' well-known stories from Acts). Checked programmatically before every import: built a set of every existing question prompt (original content + all prior batches) and diffed each new batch against it — zero duplicate prompts across all 756 new questions. New Gospel/Acts/Revelation content instead covers book-specific facts (authorship, audience, unique passages) not already asked elsewhere — e.g. Luke's Magnificat and Zacchaeus, Mark's fast pace and Peter's eyewitness basis, Ananias and Sapphira, the Jerusalem Council, the seven churches of Revelation — rather than re-asking "who betrayed Jesus" or "which apostle wrote Revelation," which were already covered.
- **Verified live for every single batch, not just compiled**: a reusable `server/src/scripts/verifyBooksBatch.ts` (takes category slugs as CLI args) starts a real session for each new level, looks up each question's actual correct answer from the database, submits it through the real API, and confirms 100% correctness plus the full sequential unlock chain to the next book — run and passed for all 64 new books across all 9 batches. Test players cleaned up after each run.
- `npx tsc --noEmit` clean after every batch. All 9 batches committed and pushed individually, not as one giant commit.
- **Final totals**: 72 total levels (8 thematic + 64 book-based), 958 total quiz questions in Bible Quiz Levels in production (202 original + 756 new — genesis/jesus each carry one extra pre-existing question over their nominal 25, a minor detail from before this pass, not introduced by it).
- **Run against production — done, verified.** All 9 `importBooksBatch.ts` commands run against the production `DATABASE_URL` (user provided the Neon connection string directly for this one-off run; never written to a file or committed). Verified two ways afterward: a direct DB query confirmed all 72 categories present with the exact expected per-category question counts and zero categories with missing content, and a live call to the real production API (`GET https://play-api-hb7f.onrender.com/api/games/levels`) confirmed all 72 levels return correctly with the right lock states. Test player created for the live check was cleaned up afterward. The levels page copy ("up to 25 questions") was already accurate for variable counts before this pass (fixed in an earlier session) — no frontend copy change was needed here.

## Completed Work — "20 Items Per Level" Catch-Up: Word Search, Scripture Puzzle, Character Guess, Story Challenge (this pass)
User feedback after the 66-book pass: the standing "20 items per level" target (Bible Quiz Levels' 25/level being the one deliberate exception) had only ever been fully applied to Bible Quiz Levels — the other 4 category-based games had drifted, with several levels well under 20. Checked directly against the database rather than assumed, then fixed all of it:
- **Word Search** — 18 of 20 puzzles were already at 20 words; only Armor of God (13) and Apostles' Epistles (15) were short. Expanded using real vocabulary from Ephesians 6:10-18 and real New Testament epistle terms. The 3 puzzles that stay honestly capped below 20 (Fruit of the Spirit=9, Ten Commandments=10, Twelve Disciples=11 — all genuinely fixed-size real lists) are unchanged, matching the existing documented exception. Verified via a standalone reimplementation of the placement algorithm (20 trials, zero failures) before importing, then via a live session confirming all 20 words in each puzzle are actually findable in the generated grid.
- **Scripture Puzzle** — genesis (1), exodus (4), jesus (4), and kings (12) were short (prophets=25 and apostles=24 already exceeded 20). Added 59 new verses, each fetched and confirmed word-for-word against bible-api.com's real KJV endpoint before being added, same standard as all prior verse content.
- **Character Guess** — all 6 categories were short (8/10/15/13/10/14). Added 50 new real Bible characters with 3 progressive clues each, no name reused across the whole game.
- **Story Challenge** — the biggest gap, all 8 categories short (8/8/8/7/5/7/5/5). Added 107 new stories with full accurate ordered event sequences (436 events total).
- **Real bug found and fixed while doing this**: the first Scripture Puzzle import omitted a `categories`/`difficulties` block since those categories already existed in the database — but `importContent`'s category-lookup map is only populated from records present in the *current* import payload, not from what's already in the database. Without it, all 59 new verses landed with `categoryId: null`, present in the database but invisible to the game. Caught via live verification (a session for "genesis" still showed only 1 verse, not 20), fixed by including the existing categories/difficulties in the payload (a no-op upsert for them) and re-running the import, which correctly backfilled the category links on the already-created rows. The same fix was applied preemptively to the Word Search and Character Guess batches.
- **A much bigger, unrelated pre-existing gap discovered while pushing to production**: production had never received three earlier local content expansions that `read.md` already listed as "done" — Scripture Puzzle's verse pool (21→70), Character Guess's character pool (20→70), and Story Challenge's story pool (20→53). Only each game's original, much smaller seed had ever actually been live; the larger expansions from an earlier session apparently were only ever run against local dev. Fixed by running the existing `importExtraGamesContent.ts` (the full canonical content file, upsert-based, safe to re-run) directly against production first, then re-applying this session's newer deltas on top in the correct order.
- **Verified live for every category of every game**, not just compiled: real sessions solved through the actual API using each item's real correct answer pulled independently from the database — 20/20 Word Search puzzle words found in-grid, 80/80 new Scripture Puzzle verses scored correct, 120/120 Character Guess names scored correct, 160/160 Story Challenge event orders scored correct. Test players cleaned up after every run.
- **Final production state, directly verified**: 20 word search puzzles, 129 Scripture Puzzle verses (genesis/exodus/kings/jesus at 20, prophets 25, apostles 24), 120 Character Guess characters (all 6 categories at 20), 160 Story Challenge stories (all 8 categories at 20).
- One transient Neon connection reset occurred mid-import for the Character Guess production push; since `importContent` runs inside a single Prisma transaction, this rolled back cleanly (confirmed via a direct row check before retrying) rather than leaving partial data — the retry succeeded cleanly.
- Added a small, unrelated fix along the way at the user's request: a show/hide toggle (eye icon) on the account page's password field, verified live via the browser-automation skill (toggles between `type="password"` and `type="text"`, zero console errors).
- **Found and fixed a real, isolated data bug while investigating an unrelated user report**: a screenshot showed one live player's Scripture Puzzle level map with level 1 completed, levels 2-3 locked, but level 4 already unlocked — skipping past two valid levels. Root cause was three historical layers stacking on one player: they played at 2:28am under the old pre-category-based Scripture Puzzle code (before that morning's rework shipped), then again at 12:44pm under the new category-based code but before production's verse-content sync (this same day) had populated Exodus/Kings, so the level list skipped straight to the next non-empty category (Prophets). Confirmed via direct DB query that exactly one player was affected, unlocked the two skipped categories to match fair progression, and removed 4 orphaned rows left over from the old architecture. The current unlock logic itself was already correct — this was a one-time migration artifact, not a recurring bug.
- **Added a global Back button** (`frontend/components/BackNav.tsx`, mounted once in `app/layout.tsx`) after user feedback that there was no reliable way to navigate back on mobile mid-game. Deliberately route-based rather than browser-history-based, since a player opening the app fresh (PWA icon tap, reopened tab, mid-level reload) has no in-app history to go back through. Every page gets a consistent "← Back" button with zero per-page wiring; hidden only on the home page. Verified live across 9 representative routes via real Playwright clicks — every one landed on the correct target.
- **Fixed Match the Verse and Flash Cards to actually serve 20 per level.** User report: Match the Verse showed far fewer than 20 pairs at low levels. Root cause was two stacked issues in both services: (1) round size was computed from a level-based ramp (3→20 pairs / 5→full-pool cards) instead of a flat 20 like every other game, and (2) a separate "difficulty window" (the subset of verses a level draws from) wasn't sized to guarantee at least as many verses as the round needed, so even a flat-20 target would still get silently truncated at low levels. Fixed both services so every level always serves exactly 20, with difficulty still escalating across levels via *which* verses are drawn from (shortest-only at level 1, full pool by level 20), never by reducing the count. Trivia was already correctly flat at 20/round. Verified live across 5 consecutive levels for both games — exactly 20 items every time, including level 1. Pure backend logic change, no data migration needed; picked up automatically on the next Render deploy.

## Completed Work — Match the Verse: Batched Gameplay UI (follow-up pass)
Once the flat-20 fix above shipped, the user reported the result was itself unplayable: all 20 pairs (40 cards) rendered on screen at once, too much to visually scan and match, versus the original design's 3-pairs-at-a-time feel. Reworked `frontend/components/MatchTheVerse.tsx` to keep the 20-pairs-per-level total (still the real source of truth for level completion) while pacing gameplay out in small groups:
- The level's 20 unique verse IDs are split into chunks of 3 (`GROUP_SIZE`), in first-appearance order; only the current group's cards are rendered at any time.
- A "Next Group →" button appears once every pair in the current group is matched, advancing to the next chunk (7 groups total per level, last group holding 2 pairs).
- A "Group X of Y" HUD badge tracks position through the groups, alongside the existing "N/20 matched" badge which stays cumulative across all groups.
- The actual match-checking and level-completion logic (`handleFlip`, `finishGame`) is untouched — grouping only changes which cards are rendered and adds the group-advance step, so the already-correct "all 20 pairs matched triggers completion" behavior still works regardless of group boundaries.
- Verified live: fresh session renders 6 cards (3 pairs, not 40), correct "0/20 matched" overall total, zero console errors, `tsc --noEmit` clean.
- Follow-up: the first cut labeled the per-screen indicator "Group 1 of 7", which the user flagged as confusing since 7 is an internal detail (20 pairs / 3 per group), not the level's real total. Changed the label to "Pairs A-B of 20" so every number on the page reads against the actual per-level count. Gameplay logic unchanged, label only.
- **Second follow-up, direct user instruction**: the real target was 3 pairs per page across 20 pages, i.e. 60 pairs per level, not 20. Raised `PAIRS_PER_LEVEL` from 20 to 60 in `verseMatchService.ts`; the existing difficulty-window guard scales off that constant automatically, and the frontend's group count/totals already derive from the actual cards array so no frontend change was needed. This is a deliberate, explicit exception to the "20 items per level" standard, scoped to Match the Verse only. Verified live on a fresh session: "Pairs 1-3 of 60", "0/60 matched", 6 cards on the first page, zero console errors.

## Completed Work — Memory Verse: 10 Levels x 20 Verses (this pass)
Memory Verse had no level concept at all -- every session grabbed a flat 3 verses ordered by createdAt desc, no progression, no level badge. Direct user instruction: give it levels like the other games, 20 verses per level across 10 levels (200 verses total).
- Added `pickMemoryVerseLevel()` to `gameService.ts`, mirroring the same pattern already proven for Match the Verse / Flash Cards: level derives from how many `memory_verse` sessions this player has completed (capped at 10), every level always serves a full 20 verses, difficulty escalates via which verses are drawn from (shortest first) rather than by varying the count, with the same difficulty-window guard so a low level can't silently serve fewer than 20.
- `startMemoryVerseSession` now returns the derived `level` alongside the verses; frontend (`MemoryVersePreview.tsx`) shows a "Level X/10" badge next to the existing "N/20" verse counter, matching the badge style already used elsewhere. No batching UI needed here -- Memory Verse already shows one verse per screen with Next/Previous, so it never had Match the Verse's "everything on screen at once" problem.
- The shared `BibleVerse` pool only had 129 active verses, short of 200. Added 71 new real KJV verses, each fetched and verified word-for-word against bible-api.com's KJV endpoint before import (same standard as every prior verse addition), under a new `memory-verses` category so Scripture Puzzle's existing 6 categories (with their already-verified 20/20/20/20/25/24 counts) stayed untouched. Total active verse pool confirmed at 200 in production after import.
- Considered and deliberately skipped adding a `GET /memory-verse/level-map` endpoint (the pattern the other 3 procedural games use for their separate pre-game map page) since Memory Verse has no such map page and nothing would have called it -- avoided shipping unused API surface.
- Verified live on a fresh guest session (after importing the same 71 verses into the local dev database too, which had also drifted to 129): "Level 1/10", "1/20", a `totalQuestions` of 20 on the created session, zero console errors. Both `tsc --noEmit` passes clean.
- **Follow-up, user question**: "what happened to snake ladder for the memory verse?" -- the first pass deliberately skipped the winding-path level-select map (the same screen Match the Verse/Flash Cards/Trivia show before gameplay), reasoning nothing would call it since Memory Verse wasn't in the original "Snake-Path Level Maps" plan's game list. That was the wrong call once Memory Verse actually had levels -- it should look and behave like every other level-based game. Fixed by re-adding `getMemoryVerseLevelMap()` + `GET /memory-verse/level-map` (same shape as the other 3 procedural games) and restructuring the frontend routing to match exactly: `/memory-verse` is now the level map (reusing `ProceduralLevelMap`/`SnakeLevelMap` as-is, zero new map code), gameplay moved to `/memory-verse/play`. `BackNav.tsx` updated so `/memory-verse/play` backs up to the map instead of `/games`. Verified live end-to-end: map shows "0/10 complete" with Level 1 unlocked, clicking Start navigates to `/memory-verse/play` showing "Level 1/10", "1/20", zero console errors.

## Completed Work — Full Swagger Documentation Coverage (this pass)
User question ("is everything in swagger documentation?") prompted checking coverage directly against the live `swaggerSpec` object rather than assuming -- found 3 whole route files with zero `@swagger` annotations: `adminRoutes.ts` (22 endpoints), `authRoutes.ts` (2), `parentAuthRoutes.ts` (5), 29 endpoints total out of 65. Documented all of them:
- Added a `bearerAuth` HTTP-bearer securityScheme to `swagger.ts`, applied to every route actually gated by `authenticateAdmin`/`authenticateParent`.
- Added 33 new schemas (admin auth, parent accounts, and full admin content-management CRUD: categories, difficulties, media, quiz questions, Bible verses, bulk imports, import history) -- every field checked against the real Prisma models and service return shapes, not guessed.
- Documented all 29 previously-missing endpoints with full request/response `$ref`s, matching the existing style used by `gameRoutes.ts`/`miniGameRoutes.ts`.
- **Found and fixed a real bug while writing these docs**: `GET /auth/me` was returning the raw Prisma `adminUser` record (including `passwordHash`) and raw `adminSession` record (including `tokenHash`) directly from `req.adminUser`/`req.adminSession`. Fixed to return the same safe subset `loginAdmin()` already returns (id/email/role, session id/expiresAt), matching `parentAuthController.me`, which was already safe. Confirmed no frontend code depends on the extra fields (no admin panel frontend exists yet), so this is a pure fix with no behavior change for any real consumer.
- Verified by importing the actual `swaggerSpec` object at runtime (the same singleton `app.ts` serves): 54 documented paths, 64 path+method entries, 88 schemas. The only endpoint left out is `GET /health`, a plain liveness probe with no meaningful request/response shape -- every real business-logic endpoint across all 7 route files is now covered. `tsc --noEmit` clean; `/api-docs/` loads 200 on the live dev server.

## In Progress
- Image upload endpoint for admin content management (not yet implemented; `server/uploads/` exists but nothing writes to it).
- Admin dashboard frontend (not started) — increasingly needed now that there are 20 quiz levels' worth of content across 6 different content types (questions, verses, characters, stories, word search puzzles) all managed by hand-edited JSON + import scripts.
- Production database needs the new migration (`20260808021106_add_content_progress_and_milestone_badges`) applied — happens automatically on next Render deploy via `render-start`'s `prisma migrate deploy` step, same as every migration before it — and the milestone badges seed script run once manually afterward (see above).

## Not Started — Larger Platform Expansion (flagged, not silently attempted)
The user described an even bigger platform vision in chat beyond what's now built (not yet reconciled into `prep.md`):
- ~~Named player accounts with login~~ — **done**, see "Roadmap: Mobile App Store Launch" Phase 2 above (parent account + kid profiles, email/password). Guest play (no account) still works unchanged for anyone who doesn't sign up.
- ~~Milestone rewards~~ — **done**, see "Snake-Path Level Maps + Milestone Rewards" above. Coins/currency, leaderboards, and question review/revisit history still don't exist in the schema or API.
- Admin dashboard UI (categories/levels/questions/verses/hints/images/audio/player accounts/leaderboard/achievements management) — the backend CRUD APIs mostly exist for categories/questions/verses/media, but there is no admin frontend at all, no audio upload support, and no hint-authoring UI (hints are currently generated programmatically — eliminating a wrong option, or revealing the next word/clue — not authored per-item).
- A general "introduce new games without changing the architecture" plugin-style system — the current architecture has one bespoke service per game; no generic game-engine abstraction exists.
- ~~Swagger/OpenAPI docs for the 6 new game route files~~ — **done**. All 16 endpoints across the 7 games sharing `miniGameRoutes.ts` (Match the Verse, Flash Cards, Scripture Puzzle, Character Guess, Story Challenge, Word Search, Daily Challenge) now documented with full request/response schemas, matching `gameRoutes.ts`'s existing `$ref`-based style rather than `levelRoutes.ts`'s lighter inline style. Response shapes verified against the actual service return values (not guessed) before writing schemas. Verified the spec genuinely loads at runtime with all 16 paths present (51 total schemas, 27 total paths, 0 missing) — the same rigor as when `swagger.ts` was originally rebuilt after its corruption incident, not just "the file compiles." ~~Remaining undocumented: `adminRoutes.ts`, `authRoutes.ts`, `parentAuthRoutes.ts`~~ — **done**, see "Full Swagger Documentation Coverage" below. Every route in the app is now documented.

This remains a multi-phase build. Flagged to the user for prioritization rather than half-implementing it.

## Environment
- Frontend stack target: Next.js + TypeScript + Tailwind CSS.
- Backend stack target: Express + TypeScript.
- Database target: PostgreSQL with Prisma.
- Local backend environment uses `server/.env`.
- **Production**: frontend `https://play-frontend-static.onrender.com` (a Render Static Site — see "Eliminated Frontend Cold Starts" pass below; the original `play-frontend` Web Service and its `play-frontend-cz51.onrender.com` URL are retired and deleted) and backend `https://play-api-hb7f.onrender.com` on Render. Database is **Neon** Postgres 17 (`us-west-2`, direct/non-pooled connection) as of the Render→Neon migration below — Render's old `play-db` has been fully decommissioned and deleted. See "Roadmap: Mobile App Store Launch" Phase 1 for how the Render services were originally set up, and "Database Migration: Render Postgres → Neon" below for the migration itself.

## Issues or Risks
- Prisma generate currently warns that the `package.json#prisma` config is deprecated in Prisma 7.
- Admin bootstrap credentials are still optional env values and must be set before seeding an admin user.
- Content and game data must remain database-driven, not hardcoded.
- `server/.env` defines `OPENAI_API_KEY` but no code in the backend currently reads or uses it; leaving it as-is since it may be for a planned feature, but flagging it as unused.
- No admin login exists yet (bootstrap credentials still unset), so the admin content API cannot be exercised through auth until they're added.
- ~~Seed content is minimal~~ — content is now substantial and real: 200 quiz questions (25 × 8 levels), 21 KJV verses, 20 word search puzzles, 20 characters, 20 stories (102 events). Still authored by Claude rather than a subject-matter reviewer — worth a human accuracy pass before this is truly public-facing, though the KJV verse text has been specifically double-checked against known wording.
- ~~No persistent player identity across the app~~ — **fixed.** Added `lib/player.ts` (localStorage helpers) and `hooks/useGuestPlayer.ts` (ensures one guest player id exists per browser, reused on every load instead of creating a new one). `QuizPreview` and `MemoryVersePreview` are now self-contained client components that use that persisted id to start their own game session client-side, instead of `quiz/page.tsx`/`memory-verse/page.tsx` creating a throwaway player server-side on every request. As a side effect this also let both pages go back to plain static shells (`next build` shows `○ Static` again) since the mutation moved into the browser — no `force-dynamic` workaround needed.
  - Player creation now happens via `PlayerAvatarSetup.tsx` on the `/games/[slug]` gate (nickname + Boy/Girl avatar), the first time a guest picks a game — see "Game Selection & Onboarding Flow" above. Known limitation carried over: there's no way to rename or re-identify as a different player later (no "switch player" UI) — acceptable for now, flagged here for later.
- Memory verse correctness check is an exact, case/whitespace-normalized string match against the full verse text — a single typo fails it. Not a bug (matches the current design), but worth revisiting for a "positive feedback, not punishment" UX per `prep.md`.

## Database Migration: Render Postgres → Neon (In Progress, Gated)
User-approved, explicitly gated migration of the production database from Render's managed Postgres to Neon, keeping the Express backend and Next.js frontend on Render unchanged. Architecture requires no code changes: `schema.prisma` already uses `url = env("DATABASE_URL")`, no hardcoded connection strings exist anywhere in the codebase (verified by grep), and all 5 migration files are plain portable Postgres DDL.
- **Step 1 (backup) — done, verified.** Local `pg_dump` was version 16.11 while Render's `play-db` runs Postgres 17 — an older client dumping a newer server is unsupported, so used a version-matched Postgres 17 `pg_dump` via Docker instead (`docker run --rm postgres:17 pg_dump -Fc ...`) against Render's external connection string. Output: `backups/play_render_backup_20260808_001208.dump` (176 KB, custom format). Added `backups/` to `.gitignore` *before* writing the file (real user data: hashed passwords, parent emails, player records — must never be committed). Verified two ways: file exists with non-trivial size, and a read-only `pg_restore --list` confirmed the archive structurally lists all 28 real tables (including `_prisma_migrations`), indexes, constraints, and FKs — not just a non-zero exit code.
- **Step 2 (Neon project) — done.** User created the Neon project ("Play") via dashboard: Postgres 17 (matches Render exactly), region `us-west-2` (Oregon, matches Render's region), Neon Auth toggle left **off** (app already has its own `ParentAccount`/`ParentSession` auth; enabling Neon Auth would add an unrelated, unneeded schema/service). Confirmed the connection string given (`...@ep-wispy-feather-afx8gl7u.c-2.us-west-2.aws.neon.tech/neondb`) has no `-pooler` in the hostname — it's already the direct (non-pooled) connection, which this app needs for its Prisma interactive `$transaction()` blocks.
- **Step 3 (restore) — done, verified.** Confirmed live via `psql`: Neon server is Postgres 17.10, exact match with the backup's origin — zero version risk. Confirmed target database was empty before restoring (`\dt` → no relations). Ran `pg_restore` (Postgres 17 Docker image) against the Neon connection string from `backups/play_render_backup_20260808_001208.dump`. Restore completed with 35 warnings, all the same benign pattern (`role "play_db_jwf4_user" does not exist` — Render's DB role doesn't exist in Neon, which uses `neondb_owner`; this only affects ownership/ACL metadata statements, not schema or data). Verified real data landed by row count: 202 QuizQuestion, 21 BibleVerse, 20 WordSearchPuzzle, 20 BibleCharacter, 20 BibleStory, 3 ParentAccount, 10 PlayerProfile, 5 `_prisma_migrations` rows — matches production.
- **Step 4 (Prisma check) — done.** `npx prisma migrate status` run with `DATABASE_URL` pointed at Neon via a one-off shell export (did **not** edit `server/.env`) → `Database schema is up to date!`, all 5 migrations recognized. Prisma is fully satisfied with the Neon copy.
- **Step 5 (local smoke test against Neon) — done, full pass.** As an extra safety buffer before touching the live Render env var, ran the Express backend locally on an isolated port (5050, since 5000 was already occupied by another dev process) with `DATABASE_URL` overridden to Neon via a one-off shell export (never touched `server/.env`). Results:
  - `GET /api/health` → `{"success":true,"message":"API is healthy"}`.
  - Real signup/game-session flow exercised end-to-end: `POST /parents/signup` (new parent account created in Neon) → `POST /parents/children` (child profile created) → `POST /games/quiz/sessions` (pulled a real restored question from Neon) → `POST /games/quiz/answers` (correct answer recorded, session completed, XP/star/level-progress all updated correctly). This specifically exercised the Prisma interactive `$transaction()` reward-logic path flagged earlier as the one real risk area — it worked cleanly, confirming the direct (non-pooled) Neon connection string is the right choice.
  - Independently verified via a direct SQL join (`ParentAccount` → `PlayerProfile` → `GameSession` → `GameSessionAnswer`) that the data was really persisted correctly in Neon, not just returned by the API.
  - Cleaned up the test parent account afterward (`DELETE FROM "ParentAccount" WHERE email LIKE 'neon-migration-test%'`, cascades to player/session/answer via existing `onDelete: Cascade` rules) so no test data lingers in what becomes the production Neon database. Confirmed 0 rows remain matching the test email. Stopped the local test server process.
- **Step 6 (render.yaml detach) — done.** Before the live cutover, changed `play-api`'s `DATABASE_URL` in `render.yaml` from `fromDatabase: { name: play-db, ... }` to `sync: false` (same pattern already used for `ADMIN_BOOTSTRAP_EMAIL`/`PASSWORD`). This was necessary, not cosmetic: leaving the `fromDatabase` binding in place while manually overriding the value in Render's dashboard would have meant the *next* Blueprint sync — including this project's routine auto-push-on-every-commit workflow — silently reverted `DATABASE_URL` back to `play-db`'s connection string and broken production. Committed and pushed; this alone changed nothing live (the value stayed whatever it currently was until manually updated).
- **Step 7 (live cutover) — done, by the user, in Render's dashboard.** User manually set `DATABASE_URL` on the live `play-api` service to the Neon direct connection string (Environment tab). Render auto-redeployed; deploy log showed `5 migrations found in prisma/migrations` / `No pending migrations to apply`, matching Neon's already-verified state.
- **Step 8 (live verification) — done, full pass.**
  - `GET https://play-api-hb7f.onrender.com/api/health` → healthy.
  - Full live signup/game-session flow against production: signup → add child → start quiz session → submit correct answer → session completed, XP/stars/level all correct.
  - **Definitively cross-verified the cutover**, not just inferred it: queried Neon directly for the new test account (found) and queried Render's old `play-db` directly for the same account (absent, 0 rows) — proof positive that live traffic is now writing to Neon and Render's old database is receiving zero new writes.
  - Cleaned up both rounds of test data (local + live) from Neon afterward; Render's old `play-db` was never written to by any of this testing, so nothing to clean up there.
  - Live frontend (`https://play-frontend-cz51.onrender.com/account`) checked with a real headless browser: form renders correctly (email/password/sign-in), 0 console errors, 0 failed requests. (First check hit Render free-tier cold-start, a 503 "waking up" page — unrelated to the migration; retried once warm and it passed clean.)
- **Step 9 (decommission old Render database) — done. Migration complete.** User explicitly confirmed it was safe to remove (asked directly via a clarifying question first, since the initial phrasing was ambiguous) and deleted `play-db` themselves via Render's dashboard. Independently verified the deletion actually took effect by attempting a direct connection to its old external hostname afterward — connection failed outright (`SSL connection has been closed unexpectedly`), confirming it's really gone, not just assumed. Cleaned up `render.yaml` to remove the now-stale `databases: - name: play-db` block and updated the `DATABASE_URL` comment to stop referencing it. No destructive database command (`DROP DATABASE`, `prisma migrate reset`, etc.) was ever run by Claude at any point in this migration — the actual deletion was a dashboard action taken by the user themselves, as it had to be (no Render API access from this environment).
- **Final state**: production fully runs on Neon (Postgres 17, direct/non-pooled connection, `us-west-2`). Render still hosts the Express backend (`play-api`) and Next.js frontend (`play-frontend`) unchanged, per the original constraint of not rewriting the architecture. The only remaining artifact from the migration is the local backup file `backups/play_render_backup_20260808_001208.dump` (git-ignored, 176 KB) — kept intentionally as a cold snapshot of the pre-migration data, not deleted.
- Recommendation carried into the next step: use Neon's **direct** (non-pooled) connection string, not the `-pooler` one — this app uses Prisma interactive `$transaction(async (tx) => {...})` blocks throughout (reward logic, admin import), which are unreliable against PgBouncer transaction-pooling mode, and since the backend is an always-on Express server (not serverless), it doesn't need Neon's pooler for connection-count management anyway.

## Completed Work — Android APK Rebuild: Bringing It Up to Date With the Web App (this pass)
User reported downloading the Android APK from `/download` and finding it missing everything built recently. Confirmed via `git log` on `frontend/public/downloads/play.apk`: the hosted APK was last built 2026-08-07 17:44, before roughly 20 real frontend commits -- snake-path level maps + milestone rewards, session resume/continue, Character Guess/Story Challenge's category-based rework, the global Back button, Match the Verse's batched UI, Memory Verse's new 10-level/200-verse system, the account page's password toggle, and more.
- **Confirmed Docker was up first** (`play_postgres`, Postgres 17, port 55432) before touching anything local, per the user's request.
- **Real, pre-existing bug found and fixed while rebuilding, not before**: attempting the mobile static export (`npm run build:mobile`) failed outright with `Page "/scripture-puzzle/[levelSlug]" is missing "generateStaticParams()"`. Root cause: `/scripture-puzzle/[levelSlug]`, `/story-challenge/[levelSlug]`, `/who-am-i/[levelSlug]`, and `/word-search/[levelSlug]` (all four added during the "Snake-Path Level Maps" pass) were left as plain `"use client"` pages reading `useParams()` directly -- the exact pattern `/games/[slug]` and `/levels/[slug]` had already been deliberately moved off of for static-export support, back in the original Capacitor packaging pass. This had been silently blocking every mobile build since that pass shipped; nobody had actually tried to rebuild the APK since then to notice. Fixed by converting all 4 to server components with `generateStaticParams()`, mirroring the existing `/games/[slug]` split exactly. New static slug lists: `lib/scripturePuzzleLevels.ts` and `lib/characterGuessLevels.ts` (the 6 thematic categories with real content), `lib/wordSearchLevels.ts` (the 20 real puzzle slugs); Story Challenge reuses the existing `lib/levels.ts` list since it's the one collection game with content in all 8 thematic categories. Side benefit, same as before: these 4 routes also upgraded from `ƒ Dynamic` to `● SSG` on the regular website build.
- **A second, unrelated real bug found while investigating the first**: `scripturePuzzleService.ts`'s `levelCategories()` counted *any* category with at least one verse -- once Memory Verse's 10-level rework added a dedicated `memory-verses` category (sortOrder 73, 71 verses, meant purely as a verse pool for Memory Verse) that category silently became an unintended 7th Scripture Puzzle level instead of the documented 6. Fixed by restricting to `sortOrder <= 8` (the original thematic categories), which also future-proofs against the 64 per-book quiz categories ever picking up a stray verse. Low blast radius assessed, not fully audited: the leak only existed for less than a day (since the `memory-verses` category was created 2026-08-08 18:49) before this fix, so it's unlikely any real player reached a "level 7" that only appears after clearing the real 6 -- flagging this rather than claiming a full production data audit was done.
- **Rebuilt through the existing pipeline, not a new one**: `npm run cap:sync` (static export against the production API + `npx cap sync` for both platforms) → `./gradlew assembleRelease` from `frontend/android` (Temurin JDK 21 + the existing Android SDK, same as the original local Android build) → copied the resulting signed `app-release.apk` over `frontend/public/downloads/play.apk`.
- **Verified before publishing, not assumed**: `apksigner verify --verbose` on the new APK confirmed it's still validly signed (APK Signature Scheme v2, one signer); `aapt dump badging` confirmed the same identity as before (`com.playbible.app`, label "Play", `INTERNET` permission, no accidental app-id/permission drift). Both the mobile static export and a normal `next build` (the website's real deploy path) passed clean after the fix, and the actual generated HTML files for the previously-broken routes (`scripture-puzzle/genesis.html`, `word-search/creation-week.html`, etc.) were confirmed present and non-trivial on disk. New APK is ~11.5MB, up from ~5.7MB, consistent with how much content and how many screens have shipped since the last build.
- Backend fix and frontend fix committed separately from the APK binary itself (3 commits), then pushed -- the backend fix also goes live on the next Render deploy, so any live Scripture Puzzle players get the correct 6-level view going forward.
- **Not done this pass**: no device/emulator install test of the actual APK (matches the same gap flagged after the original build -- no device was connected). Recommend the user sideload the new `/download` APK on a real device to confirm it visually before considering this fully closed.

## Completed Work — QA Pass: 4 Reported Bugs Fixed (this pass)
User did a QA pass as a senior SWE and reported 4 issues. Confirmed Docker was down (`play_postgres` exited) and started it before touching anything, per the user's request. All 4 confirmed as real bugs (not perception) by reading the actual code/data before touching anything, then fixed:

1. **Question repetition -- confirmed and fixed.** Root cause: `gameService.ts`'s `startQuizSession` (backs both Quick Practice `/quiz` and Bible Trivia `/trivia`, which reuses the same session infrastructure) fetched questions with `orderBy: { createdAt: "desc" }, take: questionCount` -- always the exact same fixed set of most-recently-created questions, every session, regardless of how many hundreds of questions exist in the pool. Verified live before fixing: 3 consecutive Trivia rounds at the same difficulty returned literally identical question sets. (Bible Quiz Levels' `levelService.ts` was already correct -- each level is a fixed, intentional 25-question set with no-repeat resume logic already built in; this bug was isolated to the shared quiz-session code Quick Practice and Trivia sit on top of.)
   - Fixed with a new `pickRandomQuestionIds()`: randomly samples from the *entire* filtered pool (category/difficulty), preferring questions this player hasn't answered in their last 300 quiz answers (`GameSessionAnswer` history), falling back to the full pool only once the unseen supply genuinely runs out for that filter.
   - Verified live: 3 consecutive Trivia rounds (20 questions each, answering every question so history accrues) -- round 1 vs round 2 had 0/20 overlap (full exclusion pool available), round 2 vs round 3 had 10/20 overlap only because the "easy" difficulty pool is 51 questions total and by round 3 40 of them had already been seen (11 fresh remained, below the 20 needed) -- correctly falls back to a random draw from the full pool rather than erroring or repeating a fixed set. This is a genuine data-volume limit for that filter, not a bug.
2. **Correct answers slow to show -- confirmed and fixed.** Root cause: every answer submission (Bible Quiz Levels, Quick Practice, Memory Verse, Daily Challenge) ran roughly 7-9 fully sequential DB round trips before responding -- the player-XP update, the answer row insert, and the session update all waited on each other despite touching unrelated rows, and `awardProgressRewards()` (run after *every* answer, not just level completions) re-fetched the player it had just been handed and then ran its four independent badge/avatar lookups one at a time. Locally against Docker Postgres each round trip is cheap enough to not be very noticeable, but the same chain against production's real network-hop Neon connection adds up to real, felt delay -- exactly the "slow to show" the user reported.
   - Also found and fixed while in this code: `gameService.ts` had its own separate, duplicate copy of the reward-evaluation logic (predating the shared `rewardService.ts` extraction) that didn't check `Badge.levelsCompletedThreshold` -- meaning Quick Practice/Trivia/Memory Verse answers could unlock a milestone badge purely on XP, ignoring the actual levels-completed requirement the badge was meant to enforce. Consolidated onto the one shared `rewardService.awardProgressRewards`, closing that gap and removing the duplicate.
   - Fix: `rewardService.awardProgressRewards()` now accepts an optional already-loaded player (skips the redundant refetch) and runs its independent reads in one `Promise.all` batch and its unlock writes in another, instead of one at a time. `levelService.submitLevelAnswer`, `gameService.submitQuizAnswer`/`submitMemoryVerseAnswer`, and `dailyChallengeService.submitDailyChallengeAnswer` all restructured the same way: the player-update/answer-insert/session-update writes run in parallel (none reads another's result), and the reward evaluation + progress-log write run in parallel with each other. Net effect: the per-answer hot path drops from ~7-9 sequential round trips to about 3-4 parallel batches, same queries and same results, just not waiting on each other unnecessarily.
   - Verified: local round-trip timing (296ms against Docker Postgres) confirmed correct behavior; the real latency win is proportional to network hop cost and will show up more against production's Neon connection than it does locally -- flagged honestly rather than overclaiming a locally-measured number that doesn't reflect the production path this was actually written for.
3. **Answers shown in the question -- confirmed and fixed.** Two distinct causes found by auditing all 956 active quiz questions programmatically (not spot-checked):
   - **Systemic UI leak**: `QuizPreview.tsx`, `LevelQuiz.tsx`, and `DailyChallenge.tsx` all rendered `question.scriptureReference` directly under the question prompt *before* the player answered. For a large class of questions this gives the answer away outright -- e.g. "Which prophet was swallowed by a great fish?" shown with the reference "Jonah 1:17" right underneath it. The audit found 29 such cases, and since the reference is basically always shown for every reference-bearing question, this was a live, standing leak for as long as those games have existed, not a rare edge case. Fixed by removing the reference from all three pre-answer question cards; it's still shown afterward alongside the "why" explanation (already the pattern in Quiz/Levels; added the same reveal-after-answering treatment to Daily Challenge, which previously showed no explanation or correct-answer text at all).
   - **7 authored questions that literally repeated the correct answer's exact wording inside the prompt itself** (found via exact-substring matching the correct option's text against the prompt, filtering out short answers to avoid noise) -- e.g. "What did Moses strike with his staff to bring water **from a rock**?" with correct answer "**A rock**". Reworded all 7 (`genesis-24`, `exodus-14`, `2-chronicles-04`, `song-of-solomon-01`, `daniel-05`, `zephaniah-03`, `zechariah-12`) to test the same fact without restating the answer, fixed at the source in `levels-content.json`/`levels-books-batch{2,3,4,5}.json` so a future re-import doesn't reintroduce them, and synced directly to the local database via a new one-off `server/src/scripts/fixLeakingQuestionPrompts.ts` (same pattern as `importMilestoneBadges.ts` -- **still needs to be run once against the production database**, not yet done).
   - **Known, deliberately unfixed edge case, flagged rather than silently left**: some of the 64 per-book Bible Quiz Levels categories are literally named after the question's own answer (e.g. the "Song of Solomon" level's on-screen category label sits directly above a question asking who wrote the Song of Solomon; "Joshua"'s level label sits above a question asking who succeeded Moses). This is structural to organizing quiz levels one-per-Bible-book, not a wording mistake in any one question, and fixing it properly means changing which book gets asked authorship-style questions at all, category by category, across all 64 -- a real content-authoring pass of its own, out of scope for this bug-fix pass. Left as-is and documented here rather than attempting a partial, unreviewed redesign.
4. **Render cold-start delay -- confirmed as a known free-tier limitation, mitigated (not eliminated).** Already documented as a known limitation before this pass (~60s first request after idle). Presented the user with 3 options (free ping + friendly UI / friendly UI only / paid always-on Render plan); user chose the free option. Implemented:
   - `.github/workflows/keep-alive.yml`: a GitHub Actions scheduled job pinging `https://play-api-hb7f.onrender.com/api/health` every 10 minutes, keeping the backend from spinning down during normal usage hours without paying for an always-on plan. Not a guarantee -- GitHub's cron schedule is best-effort and Render can still idle between pings, so this reduces cold starts, it doesn't eliminate them.
   - Frontend: new `lib/coldStartStatus.ts` (tiny pub/sub) + `components/ColdStartBanner.tsx` (mounted once in `app/layout.tsx`, same pattern as `BackNav`). `lib/api.ts`'s shared `apiFetch()` now starts a timer on every request; if a request takes longer than 3 seconds it flips on a friendly "Waking up the game... this can take up to a minute the first time" banner instead of leaving the screen looking stuck or broken, auto-hiding once the request resolves. Fast requests (the overwhelming majority, especially once the keep-alive ping is live) never trigger it.
- `tsc --noEmit` clean on both `server/` and `frontend/`; full production builds clean on both (`tsc` / `next build`, 84 static/SSG pages generated). Live-verified against the local backend (Docker Postgres, not just compiled): player creation, 3-round Trivia repetition check, a real Bible Quiz Levels answer submission, and the reworded `genesis-24` prompt round-tripping correctly through the real API.
- **Not yet done**: running `fixLeakingQuestionPrompts.ts` against the production database (needs the production `DATABASE_URL`, same one-off pattern used for prior production data fixes in this project) so the 7 reworded prompts take effect for real players, not just locally. The code fixes (repetition, latency, pre-answer reference leak, cold-start UX) all go live automatically on the next Render deploy; this one data-only fix needs its own manual run against production, same as `importMilestoneBadges.ts` before it.

## Strict QA Re-Verification of the Above Pass (this pass) — found one fix was incomplete
User asked for an adversarial re-verification, not a self-report, of the 4 fixes above: re-read the actual code, started fresh Docker/backend/frontend instances, and ran live tests rather than trusting the original summary. No automated test suite exists anywhere in this repo (checked both `package.json`s and the whole tree for `*.test.ts`/`*.spec.ts` -- zero), so all verification was live HTTP/DB/browser testing.
- **Repetition fix -- confirmed genuinely fixed.** Re-tested across 3 different filter shapes (no filter, difficulty-filtered, category-filtered) and multiple rounds each: 0% overlap while an unseen pool existed, correct documented fallback once a small category pool was exhausted. Bible Quiz Levels' pre-existing no-repeat guarantee reconfirmed untouched.
- **Latency fix -- functionally correct, but the actual speed claim is unverifiable in this environment.** A full 25-question level completion (with hints) showed XP/stars exactly matching the sum of per-answer awards and the unlock chain intact -- the parallelized rewrite changed nothing about results. But local Docker Postgres has near-zero query latency, so I can't honestly claim the fix *feels* faster without production's real network hop to measure against.
- **Cold-start UX -- confirmed working, with one real gap found live.** Built an actual 5-second-delay reverse proxy in front of the real backend and drove a real headless browser polling the DOM every 500ms. The banner correctly stayed hidden under 3s and appeared correctly past it, but because the page's first load makes two sequential slow calls (create player, then create session) and each restarts its own 3-second timer independently, the banner **drops for about 2.5 seconds between the two calls** before reappearing -- a real, verified flicker, not a hypothetical one. Not fixed in this pass, flagged as a known gap.
- **Answer-leak fix -- FAILED verification, this was the important finding.** Pulling the raw session-start API response directly (not the rendered UI) showed the `explanation` field is still sent to the client *before the player answers*, for Quick Practice, Trivia, and Bible Quiz Levels, and it names the correct answer outright (e.g. an `explanation` reading "Peter raised Tabitha (also called Dorcas)..." sent alongside a question whose options include "Tabitha (Dorcas)"). Quantified: **542 of 956 active questions (57%) have this leak**, and for Bible Quiz Levels specifically, all 25 questions of an entire level ship in one response before play starts. The original fix only hid `scriptureReference` in the rendered UI -- it never touched the underlying payload, and never addressed `explanation` at all. **This is still unfixed as of this write-up** -- the user redirected to a different, related content-quality issue (see below) before authorizing that specific fix. Needs its own pass: strip `explanation`/`scriptureReference` from the pre-answer payload in `gameService.startQuizSession` and `levelService.startLevelSession`, return them from the answer-submission response instead (where `correctText` already lives), and update the 3 frontend components to read post-answer explanation from the answer result rather than the original question object.

## Completed Work — Android APK Sync With Latest Web App Changes (this pass)
User asked, via an explicit scoped prompt, to sync only the already-tested web app changes into the existing Android APK -- no backend/database/API/game-logic/web-app/Android-architecture/Android-config changes.
- Ran the existing, unmodified pipeline: `npm run cap:sync` (static export against the production API, synced into the existing `frontend/android` Capacitor project) → `./gradlew assembleRelease` (existing JDK 21/SDK/keystore setup already on this machine).
- Hit one transient Windows file-lock error on the first build attempt (a stale Gradle daemon holding a handle on a freshly-copied asset file, same class of issue documented in the original Android build pass) -- stopped the daemon and retried; not a config or architecture problem.
- Verified before publishing: `apksigner verify` confirms it's still validly signed (same v2 scheme, one signer); `aapt dump badging` confirms identity unchanged (`com.playbible.app`, label "Play", `INTERNET` permission, targets SDK 36). Re-ran the *normal* `next build` (no `CAPACITOR_BUILD` flag, Render's real deploy path) afterward and confirmed it's still a standard server build, untouched.
- Replaced `frontend/public/downloads/play.apk` (grew from ~11.5MB to ~17.2MB, consistent with everything shipped since the last APK build). Diff scope was exactly one binary file, confirmed via `git status` before committing.

## Completed Work — Self-Answering Question Content Audit (this pass, separate from the API-payload leak above)
User clarified that Issue 3 above was misunderstood: the real complaint wasn't (only) the API-payload leak -- it's that **some question prompts effectively state their own answer through the wording itself** (e.g. "Jesus fed people manna with bread and fish, what did Jesus feed people with?"), independent of any API/UI concern, and asked for a real content-quality pass across the whole question bank in every game mode, done as an explicitly separate fix from the payload-leak issue (not resolved here; still open, see above).
- **Built a stronger heuristic than the earlier exact-substring check**, since the earlier one (already 0 remaining) couldn't catch paraphrased restatement. For each of the 956 active `QuizQuestion` rows, tokenized the correct option's text into significant words (stopwords/short words dropped) and measured what fraction of them also appear in the prompt. Flagged anything ≥60% overlap for manual review with full option context -- **explicitly did not blindly rewrite every flagged candidate**: pulled all 4 options for each of the 24 initial candidates and judged each on whether the *distinguishing* content (the word/name/number that actually separates the correct answer from the distractors) was withheld from the prompt or given away. Sanity-checked the threshold by also scanning the 45-59% band (65 more candidates) and confirmed every one of those was a legitimate, well-formed question (e.g. "How many days did Jesus fast in the wilderness?" / "Forty days" -- the shared word "days" is structurally required by a "how many X" question, not a leak) -- zero further fixes needed from that band.
- **18 of the 24 candidates were real leaks, fixed**; the other 6 were confirmed false positives and left untouched (e.g. "How many times a day did Daniel continue to pray?" / "Three times a day" -- sharing "times a day" is structurally unavoidable for a numeric fill-in-the-blank question and doesn't give away the number itself). Examples of real fixes: `revelation-06`'s prompt ("What vision in Revelation 12 involves a woman clothed with the sun facing a great red dragon?") was a near-100% restatement of its own correct answer -- reworded to "What symbolic vision of cosmic conflict appears in Revelation chapter 12?"; `ephesians-10` literally quoted the answer's exact KJV phrase inside the question itself, fixed the same way. `2-chronicles-12` needed more than a reword -- its correct *option text* ("Disaster/judgment on Judah") was itself a vague near-restatement of the old prompt, so both were rewritten together, grounded in what 2 Chronicles 34:23-25 actually says (the curses written in the Book of the Law, not a specific named disaster the text doesn't actually mention).
- **Caught and fixed 2 of my own rewrites that reintroduced the same problem**: re-running the audit after the first round of fixes found `2-chronicles-12`'s and `isaiah-13`'s new prompts had accidentally created fresh high-overlap matches (I'd worded the replacement using the same distinctive words as the correct option). Reworded both again, re-ran the audit, and confirmed convergence -- only the same 6 known-legitimate false positives remained.
- **Extended the same audit to `BibleCharacter` clues** (the "Who Am I?" game), since the user asked for all game modes, not just quiz questions -- checked whether any character's own name (or a significant word from a multi-word name) appears in their own clue text. Found and fixed 2 of 120: `joseph-the-carpenter`'s clue 1 literally said "I worked as a carpenter" (part of his own compound name), and `mark-apostle`'s clue 1 said "I am also called John Mark" -- handing over the exact string the free-text guess is matched against (`characterGuessService.submitCharacterGuess` does an exact normalized-string match against `character.name`, so this was a guaranteed giveaway, not just a soft hint). Reworded both to preserve the real clue (Joseph's trade, Mark's dual name) without stating the name itself.
- **Other game modes checked and confirmed not applicable**: Memory Verse (recalls full verse text, no MC options), Match the Verse/Flash Cards (matching/self-recall, no prompt-reveals-answer structure), Scripture Puzzle (reorders scrambled verse words), Story Challenge (reorders events, no answer-choice prompt), Word Search (word list, no prompt).
- **Known, deliberately unfixed structural edge case** (same one flagged in the original 7-question pass): some of the 64 per-book category labels shown on-screen during play are literally the question's own answer (e.g. "Song of Solomon" as the level title sitting above a question about who wrote it). Not touched in this pass -- fixing it means redesigning which book gets asked authorship-style questions, category by category, across all 64, which is a real content-authoring project of its own.
- Fixed at the source in `levels-books-batch{1,2,4,5,7,8,9}.json`, `character-guess-expansion-batch1.json`, and `extra-games-content.json` so future re-imports don't reintroduce any of this; synced to the local database via two new one-off scripts, `server/src/scripts/fixSelfAnsweringQuestions.ts` and `fixCharacterClueLeaks.ts` (same pattern as `fixLeakingQuestionPrompts.ts` -- **both still need to be run once against the production database**, not yet done).
- **Verified live against the real API, not just the database text**: started real sessions for 4 of the fixed levels (Leviticus, Judges, Revelation, 2 Chronicles -- the last one specifically because its correct *option text* changed, not just the prompt), confirmed the new prompts render correctly and the correct answer (including the new option text) still validates as `isCorrect: true`. Character clue fixes verified by re-running the audit script to 0 remaining leaks rather than a full live gameplay round (lower-risk, plain-text-field change, no options/matching-logic touched). `tsc --noEmit` clean.
- **Explicitly kept separate from the still-open API-payload leak** per the user's instruction -- this pass did not touch `gameService.startQuizSession`, `levelService.startLevelSession`, or any frontend rendering code; it only changed question/clue *content*.

## Completed Work — Self-Answering Question Audit, Round 2: Stricter Re-Confirmation (this pass)
User asked to "confirm it very well" that no questions leak their own answer -- explicitly not satisfied with a repeat of the earlier claim. Treated as a real request to re-verify adversarially, not just re-assert. This found the first pass's heuristic (raw word-overlap percentage against the correct answer) had real, meaningful gaps.
- **Built a stricter, more precise test**: for each of the 956 active questions, checked whether a word shared between the prompt and the correct answer also appears in *at least one* wrong answer. If it appears in **no** wrong answer, the correct option can be identified by pure text matching against the prompt, with zero Bible knowledge required -- a much more precise signal of real exploitability than a raw overlap percentage, since it directly answers "can a word-matcher get this right without knowing anything."
- **This flagged 114 candidates, not 24.** Manually reviewed every one against its real distractor options (again, not blindly fixing all 114): the large majority were legitimate -- a shared word was almost always a generic recurring Bible-quiz noun (Israel, king, God, Temple, Christ, Lord) doing necessary topic-framing (a question about "which kingdom of Israel" has to say "Israel"), not the actual tested content. The real signal turned out to be **multi-word phrase matches** and **rare/specific single terms** (redeemer, potter, Preacher, Samaritan, Holy Spirit) that functionally identify one specific verse/quote on their own.
- **26 further real leaks found and fixed** across 3 iterative rounds (found more on each re-run, including from my own rewrites -- same failure mode as the first pass, caught the same way): e.g. `matthew-11`'s prompt said "using birds and flowers as examples" while only the correct option mentions birds/flowers, so a player never needed to know Jesus' actual teaching, just needed to match "birds and flowers"; `revelation-10` asked about "after the first heaven and earth pass away" then the answer was literally "a new heaven and a new earth"; `parables-05`'s prompt named "the Good Samaritan story" directly, handing over the one-word answer via the story's own commonly-known title.
  - `matthew-11` needed **two** rewrites -- the first replacement ("about worry, pointing to how God provides for the rest of creation") still shared "worry" and "god" with the only option using those words; caught on the next re-run and reworded again to remove all of it.
- **Ran the full three-check suite to convergence, not just once**: exact-substring check (0), the original percentage-based check (down to the same 4-6 known-legitimate false positives it converged to before), and the new stricter unique-word check (down from 114 to 4 remaining, all reviewed and confirmed legitimate: `malachi-05`/`mark-08` name the story's subject, not its content; `1-peter-03`/`2-chronicles-12` share only the verse's own necessary object noun, with the real discriminator -- the verb or the direction -- genuinely absent from the prompt).
- **Re-verified the original 18+2 fixes from round 1 are still intact** (all 25 prompts + the one changed option text, diffed programmatically against expected values -- 0 mismatches) and **re-ran the character-clue check** (0/120, still clean) before declaring this done, since a regression in either would have made the "confirm it very well" claim false.
- **Live-verified 8 of the round-2 fixes against the real API** (Leviticus, Job, Isaiah, Matthew, Romans, Revelation, Parables, Amos) -- new prompts render correctly through a real session, correct answers still validate as `isCorrect: true`. `tsc --noEmit` clean.
- Fixed at the source in `levels-content.json` and `levels-books-batch{1,2,3,4,5,6,7,8,9}.json`; synced to local DB via a new one-off `server/src/scripts/fixSelfAnsweringQuestionsRound2.ts` (same pattern as before -- **needs a run against production**, not yet done, alongside the still-pending round 1 scripts).
- **Honest remaining state, not overclaimed**: 0 exact-substring leaks, 0 character-clue leaks, and 0 remaining cases where a correct answer's word is absent from every wrong answer *except* 4 judged-and-documented legitimate ones (necessary subject-naming or category framing, not the tested content). This is now a materially stronger and more adversarially-tested claim than round 1's, not just a repeat of it. The known structural exception (per-book category labels matching their own answer, e.g. Song of Solomon) remains unfixed and documented, unchanged from round 1. The API-payload leak (explanation/scriptureReference sent before answering) remains separately unfixed, per the user's explicit instruction to keep the two issues apart.

## Completed Work — Self-Answering Question Audit, Round 3: Fix Every Flagged Candidate (this pass)
User's response to round 2's "4 legitimate false positives left" finding: fix all of them, zero exceptions, since the app will be QA-reviewed and nothing should be left to a judgment call. Reworked all 90 remaining flagged questions from round 2's 114-candidate list, not just the 26 already fixed.
- **Went through the full remaining list systematically, file by file** (`levels-content.json` + all 9 `levels-books-batch*.json` files), rewording every prompt so the word it shared with its own correct answer no longer appears in the prompt at all -- including cases previously judged acceptable, like `hosea-07` ("Which kingdom of Israel did Hosea prophesy to?" sharing "Israel" with its answer) rewritten to "Which of the two divided Hebrew kingdoms..." so the answer options alone carry the specific names.
- **Same failure mode as round 2 recurred, caught the same way**: re-running the audit after the first batch of 90 fixes found 4 new overlaps introduced by the rewording itself (e.g. rewording `deuteronomy-01` to say "God's people" introduced a fresh match on "God"). Fixed those, re-ran again, found one more (`jonah-13`'s replacement text still said "Nineveh," matching its own quoted answer), fixed that too. Converged only after re-running the full audit and getting a clean pass with nothing left to fix, not after a fixed number of rounds.
- **Final state, verified fresh from a clean audit run**: **0 flagged** out of 956 active questions on the strict unique-word-match test (down from 114 at the start of round 2), 0 exact-substring leaks, 0 character-clue leaks -- re-confirmed, not assumed, since character clues weren't touched this round but a stale claim would still have been dishonest if left unchecked.
- **Consolidated into one sync script that reads from the source of truth**: `server/src/scripts/fixSelfAnsweringQuestionsRound3.ts` doesn't hardcode the 90 reworded prompts as string literals (unlike rounds 1 and 2's scripts) -- it reads each slug's current prompt directly out of the JSON data files at run time, so it can't drift out of sync with the actual source content and stays safe to re-run. **Still needs a run against production**, alongside the still-pending round 1 and round 2 scripts.
- **Live-verified 10 of the fixes against the real API** across a spread of both Old and New Testament levels (Exodus, Esther, Psalms, Daniel, Amos, Jonah, Matthew, Hebrews, Revelation, 1 Peter) -- new prompts render correctly through a real session, correct answers still validate as `isCorrect: true`. `tsc --noEmit` clean.
- **What's left, disclosed rather than hidden**: the one structural exception carried over from round 1 (per-book category labels that are themselves the answer, e.g. "Song of Solomon" as a level title above a question about who wrote it) is unchanged -- fixing it means redesigning which book gets which style of question across all 64 book categories, a real content-authoring project distinct from a wording fix. The separate API-payload leak (explanation/scriptureReference sent before answering) also remains untouched, per the user's standing instruction to keep it a separate fix.

## Completed Work — Fixed the Two Remaining Answer-Reveal Issues: Category Labels + API Payload (this pass)
User asked for both issues flagged as "left" above to actually be fixed: the per-book category label problem (structurally, not by rewording) and the API payload leak (nothing answer-revealing sent before the player answers, across all game modes, including not exposing all 25 Bible Quiz Levels questions up front).

**1. Category/level label structural fix.**
- Audited all 72 categories properly this time: for every active question, checked whether a significant word from its own category's name appears in the correct answer's text but in *none* of the wrong answers -- the same "unique among the real options" methodology already validated in the self-answering-question audits, now pointed at the category name instead of the question's own prompt. A looser first-pass heuristic over-flagged 13 categories (e.g. "Psalms" against "Psalm 23" -- the category name doesn't reveal *which* psalm, so it isn't really a leak); the rigorous test correctly narrowed this to exactly **6 real cases**, manually confirmed against full option context: `joshua-01` (answer "Joshua"), `1-samuel-15` (answer "Samuel's spirit"), `song-of-solomon-01` (answer "Solomon"), `john-15` (answer literally parenthesizes "(John)"), `revelation-16` (answer "The Revelation of Jesus Christ"), and `philippians-10` (answer "Philippi" -- a demonym match the exact-word test alone missed, added via a small explicitly-curated exception list rather than a general stemming rule that would have reintroduced the Psalms-style false positives).
- **Fixed structurally, not by rewording**: new `server/src/utils/categoryLabelReveal.ts` (`categoryNameRevealsAnswer()`) computes this *dynamically* server-side for every question, every time -- not a hardcoded list of "bad" questions. `levelService.ts` calls it while building each question's response and returns a `hideLevelLabel: boolean` flag. `LevelQuiz.tsx` shows the real book name as the in-play header for all other questions (keeping the pedagogical value of "you're learning about Genesis") but swaps in a neutral "BIBLE QUIZ" label for the ~6 specific questions where showing it would hand over the answer. Book-based organization, the level map, and progression are completely untouched -- this only changes what text renders in the header for a handful of individual questions.
- Self-maintaining: since the check runs live against the real options every time, it automatically covers any question added or changed later without needing another manual audit pass.

**2. API payload leak -- fixed for real this time, not just hidden in the UI.**
- Confirmed via a fresh audit of every game service (not just the three named) that only three had the leak: `gameService.ts` (Quick Practice, Trivia), `levelService.ts` (Bible Quiz Levels), and `dailyChallengeService.ts` (Daily Challenge). The other 8 game services (Word Search, Scripture Puzzle, Character Guess, Story Challenge, Match the Verse, Flash Cards) don't have an analogous pre-answer answer field to begin with. Memory Verse was deliberately left alone -- showing the verse text before answering is the actual point of a memorization game, not a leak.
- `gameService.startQuizSession` and `dailyChallengeService.getTodayChallenge` no longer include `explanation`/`scriptureReference` in the question object at all -- both fields moved to `submitQuizAnswer`'s and `submitDailyChallengeAnswer`'s result instead, returned only once the question is actually answered.
- **Bible Quiz Levels rearchitected, not just trimmed**: `levelService.startLevelSession` previously returned all 25 questions (with explanation/scriptureReference) in one bulk response. Now it returns exactly **one** question at a time -- prompt, image, and shuffled options only, with no `isCorrect` flag, no explanation, no scriptureReference. The session's shuffle order is now fixed once at creation and stored in `GameSession.metadata.questionOrder`, so `submitLevelAnswer`'s response can compute and include the correct `nextQuestion` (or `null` once the level is finished) without ever exposing more than the current question's data. `explanation`/`scriptureReference` moved into the answer result, revealed only after submitting. A same-shape fallback (plain `sortOrder`) covers any session that predates this change so an old in-flight session still resumes correctly instead of erroring.
- Frontend: `QuizPreview.tsx`/`DailyChallenge.tsx` read `explanation`/`scriptureReference` from the answer result now, not the original question object (which no longer carries them). `LevelQuiz.tsx` was restructured around a single `currentQuestion` + `nextQuestion` handoff instead of a local array + index, with `hideLevelLabel` driving the header text per the structural fix above.
- **Verified live against the real API, not just by re-reading the code**: raw `curl` payload inspection confirmed zero `explanation`/`scriptureReference`/`isCorrect` fields anywhere in any pre-answer response, across Quick Practice, Trivia, Daily Challenge, and every Bible Quiz Levels question tested. Walked all 6 structurally-affected levels end to end (70 questions total) confirming `hideLevelLabel` fires exactly on the intended question each time and nowhere else, and that a normal level (Genesis) never sets it. Full regression pass on a real 25-question Genesis level completion: hint math correct (10→8→6→3rd hint rejected), all 25 questions served with zero duplicates, XP/stars exactly matched the sum of per-answer awards, unlock chain and `nextLevelSlug` correct. Also verified session resume: same session ID reused, `answeredOffset` correct, and the resumed question is never a repeat of one already answered. `tsc --noEmit` clean on both `server/` and `frontend/`; full `next build` clean (84 pages).
- **Deliberately not changed**: Quick Practice and Trivia still return their multiple questions in one batch (5 and 20 respectively) -- the user only asked for the one-at-a-time treatment on Bible Quiz Levels specifically, and neither of those responses contains any answer-revealing field regardless, so there's nothing left to leak from seeing the prompts in advance.

## Completed Work — Eliminated Frontend Cold Starts (this pass, follow-up to the keep-alive ping)
User reported real screenshots: the account pages rendering fine on a real device (not a responsive-design bug), but also Render's own branded "waking up" splash page (ASCII art, server logs) -- their mentor's standing requirement is that real users must never see that page at all.
- **Root cause**: `play-frontend` is a free-tier Render Web Service -- a live Node process (`next start`), independently subject to Render's idle-sleep behavior, separate from `play-api`'s own sleep clock. The keep-alive workflow added in an earlier pass only pinged the backend's `/api/health`; the frontend was never covered, so it kept falling asleep on its own and real visitors hit Render's raw splash before this app's own code (including the in-app `ColdStartBanner`) ever got a chance to load. Confirmed live before fixing anything: a direct `curl` to the frontend took 50 seconds to respond, proving it really was asleep.
- **First fix (same pass, immediate mitigation)**: extended `.github/workflows/keep-alive.yml` to also ping the frontend root every 10 minutes, matching the existing backend ping. Reduces frequency, doesn't eliminate it -- GitHub's schedule is best-effort and Render can still sleep a service between pings.
- **Second fix (the real one, same pass)**: checked whether the frontend needs a live server at all -- it doesn't. Grepped for `app/api/*` routes (zero exist; every real API call already goes to the separate `play-api` service) and confirmed every page already builds as static/SSG content. A Render **Static Site** serves pre-built files directly with no process to ever sleep, eliminating the splash-screen class of problem entirely, for free -- not just reducing its odds the way a keep-alive ping does.
- Added `play-frontend-static` as a **new** service in `render.yaml` (`runtime: static`, `buildCommand` runs the same `CAPACITOR_BUILD=1` static export the mobile app build already uses, pointed at the real production API, `staticPublishPath: ./out`) rather than modifying `play-frontend` in place -- confirmed against Render's own Blueprint docs first that an existing service's type can't be converted via Blueprint sync, only created fresh, so this was the only available path. `play-frontend` is untouched and keeps serving live traffic exactly as before; nothing breaks if the new service's first live deploy needs a follow-up fix.
- **Verified locally before touching the live config, not just built**: ran the exact static export this new service will build, served it, and drove a real headless browser against it -- `/account` renders the correct "Welcome back" sign-in form (matching the user's own screenshot), `/games` renders the full game list, zero console errors and zero failed requests on both.
- **Follow-through completed (separate pass)**: the user confirmed `play-frontend-static` deployed successfully and works live at `https://play-frontend-static.onrender.com`, then deleted the old `play-frontend` Web Service from the Render dashboard by hand (Blueprint sync can't delete an existing service). The now-dead `play-frontend` block was removed from `render.yaml` to match (Blueprint sync also can't remove a service on its own, so this had to happen either way, just to keep the file honest about what's actually deployed). `play-frontend-cz51.onrender.com` is permanently retired -- Render doesn't reassign a URL to a different service. References to the old URL updated everywhere it appeared: this file's "Production" section below, `store-listing.md`'s support/privacy-policy URLs, and `.github/workflows/keep-alive.yml` -- which also had its frontend ping removed entirely rather than repointed, since a Static Site has no live process to ever sleep and so nothing to keep warm.

## Next Steps
1. ~~Decide on a free Android distribution channel~~ — **done**: direct APK download at `/download`, live on the site.
2. Apple/Google store submission is no longer being actively pursued (explicit user decision, not a budget-driven pause) — nothing to do here unless that changes.
3. Build the admin dashboard frontend — still a real gap (6 content types managed by hand-editing JSON + import scripts).
4. Implement the admin content image/audio upload endpoints.
5. ~~Add the missing "Level N/20" badge to Word Search, Scripture Puzzle, Character Guess, and Story Challenge UIs~~ — **done**, see "Snake-Path Level Maps" pass above.
6. ~~Add Swagger docs for the 6 new game route files~~ — **done**, see "Not Started" section above for detail.
7. ~~Session resume / continue where you left off~~ — **done**, see "Session Resume / Continue Where You Left Off" pass above.
8. ~~Add the remaining 64 Bible books to Bible Quiz Levels~~ — **done**, see "Bible Quiz Levels: All 66 Books" pass above. All 66 Bible books now have a level. Still needs: running the 9 import scripts once against production (see that section for the exact commands).
9. Run `server/src/scripts/fixLeakingQuestionPrompts.ts` once against the production database — syncs the 7 reworded question prompts from the "QA Pass" section above; the code fixes in that same pass already go live automatically on the next deploy, only this data fix needs a manual run.
10. Run `server/src/scripts/fixSelfAnsweringQuestions.ts` and `fixCharacterClueLeaks.ts` once against the production database — syncs the 18 reworded question prompts (one with a changed correct-option text) and 2 reworded character clues from the "Self-Answering Question Content Audit" pass above.
10b. Run `server/src/scripts/fixSelfAnsweringQuestionsRound2.ts` once against the production database — syncs the 26 further reworded question prompts from the "Round 2: Stricter Re-Confirmation" pass above.
10c. Run `server/src/scripts/fixSelfAnsweringQuestionsRound3.ts` once against the production database — syncs the remaining 90 reworded question prompts from the "Round 3: Fix Every Flagged Candidate" pass above, bringing production to the same 0-flagged state confirmed locally.
11. Fix the still-open API-payload leak (`explanation`/`scriptureReference` sent to the client before answering, confirmed live in the "Strict QA Re-Verification" pass above) — needs its own change to `gameService.startQuizSession`/`levelService.startLevelSession` plus the 3 frontend components that read post-answer explanation from the question object today. Deliberately not done yet — the user redirected to the content-quality issue first and asked to keep the two separate.
7. Add more seed content and, when ready, set `ADMIN_BOOTSTRAP_EMAIL`/`ADMIN_BOOTSTRAP_PASSWORD` in `server/.env` to create a real admin login.